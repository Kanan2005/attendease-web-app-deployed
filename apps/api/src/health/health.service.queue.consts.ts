export const announcementOutboxTopic = "classroom.announcement.posted"

export const analyticsOutboxTopics = [
  "analytics.attendance.refresh",
  "attendance.session.ended",
  "attendance.session.expired",
  "attendance.session.edited",
] as const

export const queueBacklogMultiplier = 3
