# AttendEase Implementation Context

Use this file as the shared handoff for new chat windows.
Keep it short, factual, and implementation-focused.

## Current Status

- Current repo baseline: the original implementation track is feature-complete across student mobile, teacher mobile, teacher/admin web, QR + GPS attendance, Bluetooth attendance, session history/manual edits, reports/exports, analytics/email automation, device trust/admin recovery, Docker runtime, and release-readiness tooling.
- Current release status: `Structure/release-readiness-report.md` still records an overall `NO-GO` because real-device QR/Bluetooth signoff and production environment validation for Google OIDC, SES, Sentry, and OTEL are still pending.
- Important verified baseline: split teacher/admin browser auth now works end to end at `http://localhost:3000/login`, `http://localhost:3000/register`, and `http://localhost:3000/admin/login`; mobile reports now match backend report truth; Docker runtime is healthy; and Android emulator validation is complete for its intended scope.
- Current product-audit baseline: the repo now includes a deterministic full screenshot audit at `Structure/full-product-screenshot-audit.md` with matching mobile/web artifacts under `Structure/artifacts/full-product-audit`.
- Current UI baseline: shared mobile and web primitives now use the warmer premium reset visual system from `packages/ui-mobile` and `packages/ui-web`.
- Current branch: Git branch status unavailable in this environment because `git` resolves to the parent workspace and is not reliable for this repo handoff.
- Last updated on: 2026-03-16
- Updated by: Codex

## Reset Track Status

- Reset track is now active through `Structure/phase_promptioing/20-product-reset-micro-prompts.md`.
- Prompt 1, Prompt 2, Prompt 3, Prompt 4, Prompt 5, Prompt 6, Prompt 7, Prompt 8, Prompt 9, Prompt 10, Prompt 11, Prompt 12, Prompt 13, Prompt 14, Prompt 15, Prompt 16, Prompt 17, Prompt 18, Prompt 19, Prompt 20, Prompt 21, Prompt 22, Prompt 23, Prompt 24, Prompt 25, Prompt 26, Prompt 27, Prompt 28, Prompt 29, Prompt 30, Prompt 31, Prompt 32, Prompt 33, Prompt 34, Prompt 35, Prompt 36, Prompt 37, Prompt 38, Prompt 39, and Prompt 40 are complete.
- Reset-track marker sections are now enforced by workspace validation for both the baseline audit and the IA/auth docs that later prompts depend on.
- New source-of-truth audit: `Structure/ux-redesign-audit.md`
- Current reset-track next pickup point:
  - Reset-track implementation prompts are complete
  - next recommended action is real-device release validation for QR camera, GPS range/accuracy, and Bluetooth advertiser/scanner behavior, followed by production credential validation and release-readiness signoff
- Reset-track overview docs updated:
  - `Structure/requirements/01-system-overview.md`
  - `Structure/architecture/01-system-overview.md`
  - `Structure/requirements/02-auth-roles-enrollment.md`
  - `Structure/requirements/03-student-mobile-app.md`
  - `Structure/requirements/04-teacher-mobile-app.md`
  - `Structure/requirements/05-teacher-web-app.md`
  - `Structure/architecture/02-auth-roles-enrollment.md`
  - `Structure/architecture/03-student-mobile-app.md`
  - `Structure/architecture/04-teacher-mobile-app.md`
  - `Structure/architecture/05-teacher-web-app.md`
- `Structure/architecture/13-academic-management-and-scheduling.md`
- `Structure/architecture/14-classroom-communications-and-roster.md`

## Project Structure Refactor

- Implemented:
  - split the oversized route-facing UI/workspace layer into smaller role-specific files while preserving stable imports through the existing top-level barrels:
    - `apps/mobile/src/student-foundation.tsx`
    - `apps/mobile/src/teacher-foundation.tsx`
    - `apps/web/src/teacher-workflows-client.tsx`
    - `apps/web/src/admin-workflows-client.tsx`
  - added second-stage per-screen and per-workspace files under:
    - `apps/mobile/src/student-foundation/`
    - `apps/mobile/src/teacher-foundation/`
    - `apps/web/src/teacher-workflows-client/`
  - documented the new ownership map in `Structure/codebase-structure.md`
- Tests added or updated:
  - `apps/mobile/src/foundation-exports.test.ts`
  - `apps/web/src/workspace-exports.test.ts`
- Docs updated:
  - `README.md`
  - `Structure/testing-strategy.md`
  - `Structure/architecture/03-student-mobile-app.md`
  - `Structure/architecture/04-teacher-mobile-app.md`
  - `Structure/architecture/05-teacher-web-app.md`
  - `Structure/codebase-structure.md`
  - `Structure/context.md`
- Commands run:
  - `pnpm --filter @attendease/mobile typecheck`
  - `pnpm --filter @attendease/web typecheck`
  - additional focused tests and validation are expected after the export-smoke tests land
- Blockers:
  - no active blocker for the UI/workspace restructure itself
  - several large single-purpose backend, shared-helper, and integration-test files remain as the next structural refactor candidates; they are listed in `Structure/codebase-structure.md`
- Exact next pickup point:
  - continue with service-layer and large-test refactors starting from:
    - `apps/api/src/modules/admin/admin-device-support.service.ts`
    - `apps/api/src/modules/auth/auth.service.ts`
    - `apps/api/src/modules/academic/scheduling.service.ts`
    - `apps/api/src/modules/attendance/qr-attendance.service.ts`
    - `packages/db/src/seed.ts`

## Post-Reset Full Product Audit + Premium UI Sync

- Implemented:
  - finished the repo-wide premium presentation sync through the shared design-system packages and applied that upgraded visual language to role entry, student-mobile, teacher-mobile, teacher-web, admin-web, and teacher live-QR surfaces
  - added deterministic audit tooling for the full reset product:
    - `scripts/full-product-audit-lib.mjs`
    - `scripts/generate-full-product-audit.mjs`
    - `scripts/prepare-mobile-audit-screenshots.mjs`
    - `scripts/capture-web-audit.mjs`
    - `scripts/mobile-emulator-lib.mjs`
    - `scripts/start-mobile-emulator-dev.mjs`
  - added the root command surface for:
    - `pnpm manual:mobile:emulator`
    - `pnpm audit:matrix`
    - `pnpm audit:screenshots:mobile`
    - `pnpm audit:screenshots:web`
  - fixed local browser-to-API CORS for the host web flow so teacher/admin browser screenshots and authenticated audit capture work against `http://localhost:3000` and `http://localhost:4000`
  - refreshed the previously stale mobile audit captures for:
    - shared role entry
    - student home
    - student join classroom
    - teacher home
    - teacher classrooms list
    - teacher classroom detail
    - teacher class sessions
  - regenerated the full audit so the matrix now reflects the current captured state instead of the earlier blocked placeholders
- Tests added or updated:
  - `packages/ui-mobile/src/index.test.ts`
  - `packages/ui-web/src/index.test.ts`
  - `packages/config/src/index.test.ts`
  - `scripts/full-product-audit-lib.test.ts`
  - `scripts/mobile-emulator-lib.test.ts`
- Docs updated:
  - `README.md`
  - `guide.md`
  - `Structure/manual-check-quickstart.md`
  - `Structure/testing-strategy.md`
  - `Structure/ux-redesign-audit.md`
  - `Structure/requirements/03-student-mobile-app.md`
  - `Structure/requirements/04-teacher-mobile-app.md`
  - `Structure/requirements/05-teacher-web-app.md`
  - `Structure/architecture/03-student-mobile-app.md`
  - `Structure/architecture/04-teacher-mobile-app.md`
  - `Structure/architecture/05-teacher-web-app.md`
  - `Structure/context.md`
- Commands run:
  - `pnpm audit:screenshots:web`
  - `node scripts/prepare-mobile-audit-screenshots.mjs`
  - `node scripts/generate-full-product-audit.mjs`
  - `adb -s emulator-5554 reverse tcp:8101 tcp:8101`
  - `adb -s emulator-5554 reverse tcp:4000 tcp:4000`
  - `EXPO_PUBLIC_API_URL=http://127.0.0.1:4000 pnpm --filter @attendease/mobile exec expo start --clear --host localhost --port 8101`
  - `adb -s emulator-5554 shell am start -W -a android.intent.action.VIEW -d 'attendease://expo-development-client/?url=http://127.0.0.1:8101' com.anurag203.attendease`
  - `pnpm exec biome check --write ...`
  - `pnpm exec vitest run scripts/full-product-audit-lib.test.ts scripts/mobile-emulator-lib.test.ts`
  - `pnpm --filter @attendease/ui-mobile test -- src/index.test.ts`
  - `pnpm --filter @attendease/ui-web test -- src/index.test.ts`
  - `pnpm --filter @attendease/config test -- src/index.test.ts`
  - `pnpm --filter @attendease/mobile typecheck`
  - `pnpm --filter @attendease/web build`
  - `pnpm --filter @attendease/web typecheck`
  - `pnpm --filter @attendease/api typecheck`
  - `pnpm workspace:validate`
- Blockers:
  - no repo blocker remains for the screenshot audit or premium UI sync
  - QR camera, GPS accuracy/range, and BLE proximity still remain manual-required on real hardware even though the route screenshots now exist
  - native dev-client nested deep links on Android emulator can still land on `Unmatched Route`; base app launch plus in-app navigation remains the reliable emulator path
- Exact next pickup point:
  - if more product work is requested, continue from the now-synced audit artifact set at `Structure/full-product-screenshot-audit.md`
  - otherwise move to real-device release validation and the final GO/NO-GO decision in `Structure/release-readiness-report.md`

## Prompt 40 Summary

- Implemented:
  - executed the final reset-track regression and acceptance pass across targeted backend, mobile, web, integration, build, typecheck, runtime, and native Android validation paths
  - verified the reset-track implementation story against the current repo state instead of the earlier scaffold/reset-baseline assumptions
  - updated the shared handoff so later chats can distinguish:
    - reset-track implementation completion
    - emulator/native acceptance that is now complete
    - remaining real-device and production-environment release blockers
- Acceptance status:
  - PASS:
    - split student/teacher mobile role-entry flow
    - teacher web auth routing and QR setup/control path through automated web and backend coverage
    - teacher mobile Bluetooth setup flow
    - student QR route UX and empty/live-waiting state handling
    - student Bluetooth route UX and empty/live-waiting state handling
    - teacher mobile history, live roster visibility, manual correction UI, and report surfaces
    - teacher web history, reports, manual correction, admin recovery, and admin governance coverage through the final targeted suites
    - student mobile reports
    - admin device recovery and classroom-governance flows through final targeted suites and admin integrations
  - BLOCKED OR MANUAL REQUIRED:
    - real-device QR camera scan success and GPS distance/accuracy signoff
    - real-device Bluetooth advertiser/scanner proximity signoff
    - production validation for Google OIDC, SES, Sentry, and OTEL before release-readiness can move to GO
  - KNOWN NON-BLOCKING CAVEAT:
    - direct route deep links inside the native Android development client can still land on `Unmatched Route`; the reliable Prompt 40 acceptance path was base dev-client launch plus in-app navigation
- Tests or validation strengthened:
  - final regression pass reran targeted suites for:
    - API attendance/device policy coverage
    - mobile role-entry, student, teacher, Bluetooth, QR, device-trust, report, and route coverage
    - web auth, teacher QR/history/report flows, and admin recovery/governance flows
  - final integration pass confirmed:
    - auth
    - academic management
    - classroom roster
    - QR attendance
    - Bluetooth attendance
    - attendance history/manual edit truth
    - reports consistency
    - admin device recovery
    - admin student governance
    - admin classroom governance
  - final Android native validation proved:
    - root role-entry screen
    - teacher sign-in, home, reports, session history, correction screen, and Bluetooth setup screen
    - student sign-in, home, reports, attendance hub, QR route, and Bluetooth route
- Docs updated:
  - `guide.md`
  - `Structure/manual-check-quickstart.md`
  - `Structure/context.md`
- Commands run:
  - `which adb && adb devices -l && emulator -list-avds`
  - `POSTGRES_PORT=55432 pnpm runtime:check`
  - `pnpm test:api:targeted -- src/modules/attendance/qr-token.service.test.ts src/modules/attendance/gps-validator.service.test.ts src/modules/attendance/location-anchor.service.test.ts src/modules/devices/device-binding-policy.service.test.ts src/modules/devices/trusted-device.guard.test.ts`
  - `pnpm test:mobile:targeted -- src/mobile-entry.test.ts src/student-foundation.test.ts src/student-attendance.test.ts src/bluetooth-attendance.test.ts src/student-workflow-models.test.ts src/student-view-state.test.ts src/student-query.test.ts src/teacher-operational.test.ts src/teacher-view-state.test.ts src/teacher-models.test.ts src/teacher-query.test.ts src/teacher-routes.test.ts src/teacher-session.test.ts src/device-trust.test.ts`
  - `pnpm test:web:targeted -- src/web-auth-entry.test.ts src/web-auth-session.test.ts src/web-portal.test.ts src/web-workflows.test.ts src/teacher-qr-session-management.test.ts src/teacher-review-workflows.test.ts src/admin-device-support.test.ts src/admin-student-management.test.ts src/admin-classroom-governance.test.ts src/teacher-classroom-management.test.ts src/teacher-roster-management.test.ts`
  - `pnpm --filter @attendease/api typecheck`
  - `pnpm --filter @attendease/mobile typecheck`
  - `pnpm --filter @attendease/web build`
  - `pnpm --filter @attendease/web typecheck`
  - `POSTGRES_PORT=55432 pnpm test:api:integration -- src/modules/auth/auth.integration.test.ts`
  - `POSTGRES_PORT=55432 pnpm test:api:integration -- src/modules/academic/academic-management.integration.test.ts`
  - `POSTGRES_PORT=55432 pnpm test:api:integration -- src/modules/academic/classroom-roster.integration.test.ts`
  - `POSTGRES_PORT=55432 pnpm test:api:integration -- src/modules/attendance/qr-attendance.integration.test.ts`
  - `POSTGRES_PORT=55432 pnpm test:api:integration -- src/modules/attendance/bluetooth-attendance.integration.test.ts`
  - `POSTGRES_PORT=55432 pnpm test:api:integration -- src/modules/attendance/attendance-history.integration.test.ts`
  - `POSTGRES_PORT=55432 pnpm test:api:integration -- src/modules/reports/reports.integration.test.ts`
  - `POSTGRES_PORT=55432 pnpm test:api:integration -- src/modules/admin/admin-device-support.integration.test.ts`
  - `pnpm --filter @attendease/api exec vitest run --hookTimeout=30000 src/modules/admin/admin-student-management.integration.test.ts`
  - `pnpm --filter @attendease/api exec vitest run --hookTimeout=30000 src/modules/admin/admin-classroom-governance.integration.test.ts`
  - `adb -s emulator-5554 shell pm clear com.anurag203.attendease`
  - `adb -s emulator-5554 shell am start -W -a android.intent.action.VIEW -d 'attendease://expo-development-client/?url=http%3A%2F%2F192.168.29.11%3A8099' com.anurag203.attendease`
  - `adb -s emulator-5554 shell uiautomator dump /sdcard/window_dump.xml`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt40-root-reset.png`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt40-teacher-home.png`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt40-teacher-reports.png`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt40-teacher-session-history.png`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt40-teacher-session-detail.png`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt40-teacher-bluetooth-create.png`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt40-student-home.png`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt40-student-reports.png`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt40-student-attendance.png`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt40-student-qr-route.png`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt40-student-bluetooth-route.png`
  - `adb -s emulator-5554 logcat -d | rg "Maximum update depth exceeded|FATAL EXCEPTION|ReactNativeJS|expo-router|Unmatched Route"`
  - `pnpm workspace:validate`
- Blockers:
  - no remaining reset-track implementation blocker
  - release is still blocked on the already-known real-device QR/GPS/Bluetooth signoff and production environment validation
  - grouped multi-file integration runs can still trip the default Vitest `beforeAll` timeout on temp-DB startup; when that happens, rerun the affected suites individually or with a longer hook timeout
- Exact next pickup point:
  - reset track is complete
  - next work should be release-readiness signoff:
    - real-phone QR validation
    - real-phone Bluetooth validation
    - production credential/telemetry validation
    - final GO/NO-GO update in `Structure/release-readiness-report.md`

## Post-Prompt 40 Release-Readiness Sync

- Implemented:
  - synced `Structure/release-readiness-report.md` to the final reset-track acceptance evidence
  - updated the release report so it now records:
    - reset-track regression completion
    - native Android acceptance coverage that was completed on the emulator
    - the real current next action, which is physical-device validation rather than more emulator setup
  - updated guide-path wording earlier so native dev-client acceptance uses base launch plus in-app navigation instead of unreliable route-deep-link sweeps
- Tests or validation strengthened:
  - no new code tests were added in this sync-only pass
  - re-verified environment state with:
    - `adb devices -l`
    - `POSTGRES_PORT=55432 pnpm runtime:check`
    - `pnpm workspace:validate`
- Commands run:
  - `adb devices -l`
  - `POSTGRES_PORT=55432 pnpm runtime:check`
  - `pnpm workspace:validate`
- Blockers:
  - only the emulator is attached in the current environment, so real-phone QR/GPS/Bluetooth signoff still cannot be completed from this session alone
  - production credential validation for Google OIDC, SES, Sentry, and OTEL is still environment-blocked
- Exact next pickup point:
  - connect physical test phones and complete:
    - QR camera + GPS signoff
    - Bluetooth advertiser/scanner signoff
  - then validate production/staging credentials and telemetry wiring
  - finally update `Structure/release-readiness-report.md` with the last GO/NO-GO decision

## Prompt 39 Summary

- Implemented:
  - completed the reset-track documentation sync across:
    - requirement docs
    - architecture docs
    - `Structure/ux-redesign-audit.md`
    - `Structure/context.md`
    - `README.md`
    - `guide.md`
    - `Structure/testing-strategy.md`
  - added explicit reset implementation-status markers to the reset-touched requirement docs so
    Prompt 40 can validate against one settled product story instead of inferring whether a surface
    is still planned or already live
  - added explicit reset implementation-snapshot markers to the reset-touched architecture docs so
    platform ownership, live workflow boundaries, and remaining hardware-only caveats stay clear
  - updated the UX audit so it now records both:
    - the original pre-reset diagnosis
    - the implemented reset state plus remaining validation-only gaps
  - updated top-level docs so the repo is described as the current reset-track implementation
    instead of a scaffold/foundation-only state
  - strengthened workspace validation so doc-sync markers for the reset track now fail fast if they
    are removed later
- Tests or validation strengthened:
  - `scripts/validate-workspace.mjs` now also validates:
    - reset implementation-status markers in reset-touched requirement docs
    - reset implementation-snapshot markers in reset-touched architecture docs
    - reset-state markers in the audit, README, guide, testing strategy, and context handoff
- Docs updated:
  - `Structure/requirements/01-system-overview.md`
  - `Structure/requirements/02-auth-roles-enrollment.md`
  - `Structure/requirements/03-student-mobile-app.md`
  - `Structure/requirements/04-teacher-mobile-app.md`
  - `Structure/requirements/05-teacher-web-app.md`
  - `Structure/requirements/06-qr-gps-attendance.md`
  - `Structure/requirements/07-bluetooth-attendance.md`
  - `Structure/requirements/08-session-history-manual-edits.md`
  - `Structure/requirements/09-reports-exports.md`
  - `Structure/requirements/11-data-rules-audit.md`
  - `Structure/architecture/01-system-overview.md`
  - `Structure/architecture/02-auth-roles-enrollment.md`
  - `Structure/architecture/03-student-mobile-app.md`
  - `Structure/architecture/04-teacher-mobile-app.md`
  - `Structure/architecture/05-teacher-web-app.md`
  - `Structure/architecture/06-qr-gps-attendance.md`
  - `Structure/architecture/07-bluetooth-attendance.md`
  - `Structure/architecture/08-session-history-manual-edits.md`
  - `Structure/architecture/09-reports-exports.md`
  - `Structure/architecture/11-data-rules-audit.md`
  - `Structure/architecture/13-academic-management-and-scheduling.md`
  - `Structure/architecture/14-classroom-communications-and-roster.md`
  - `Structure/architecture/15-device-trust-and-admin-controls.md`
  - `Structure/ux-redesign-audit.md`
  - `Structure/testing-strategy.md`
  - `README.md`
  - `guide.md`
  - `Structure/context.md`
  - `scripts/validate-workspace.mjs`
- Commands run:
  - `pnpm exec biome check --write scripts/validate-workspace.mjs`
  - `pnpm workspace:validate`
- Blockers:
  - no prompt-39 documentation blocker
  - remaining reset-track open items are now final validation items for Prompt 40 plus the already
    known real-device/production release blockers outside the reset documentation story
- Exact next pickup point:
  - Prompt 40 in `Structure/phase_promptioing/20-product-reset-micro-prompts.md`

## Prompt 38 Summary

- Implemented:
  - hardened shared correction semantics so teacher mobile and teacher web now use the same
    correction-review helpers from `packages/domain/src/attendance-edit.ts` for:
    - review polling while a session is still live or still editable
    - `Mark present` / `Mark absent` action labels
    - `Will save as present` / `Will save as absent` pending labels
    - shared saved-success / already-aligned messages
  - updated teacher mobile correction review in:
    - `apps/mobile/src/teacher-operational.ts`
    - `apps/mobile/src/teacher-foundation.tsx`
  - updated teacher web correction review in:
    - `apps/web/src/teacher-review-workflows.ts`
    - `apps/web/src/teacher-workflows-client.tsx`
  - teacher web correction and report surfaces now map backend failures into concise teacher-facing
    copy instead of leaking raw generic wording
  - tightened backend duplicate/no-op correction behavior in:
    - `apps/api/src/modules/attendance/attendance-history.service.ts`
  - no-op correction saves now skip realtime counter publish and keep audit/outbox churn at zero
  - added report-consistency proof that one saved manual correction updates:
    - teacher session history
    - teacher subject rollups
    - teacher student-percentage rows
    - student self-report subject detail
    from the same corrected final attendance truth
- Tests or validation strengthened:
  - updated:
    - `packages/domain/src/attendance-edit.test.ts`
    - `packages/domain/src/index.test.ts`
    - `apps/mobile/src/teacher-operational.test.ts`
    - `apps/web/src/teacher-review-workflows.test.ts`
    - `apps/api/src/modules/attendance/attendance-history.integration.test.ts`
    - `apps/api/src/modules/reports/reports.integration.test.ts`
  - integration proof now explicitly covers:
    - duplicate/no-op correction save behavior
    - corrected report/history truth consistency after a saved manual attendance change
  - native Android validation now proves:
    - teacher sign-in still works in the native debug build
    - teacher session history still exposes `Review And Correct`
    - teacher session detail shows the correction-open state on device
    - absent student rows expose `Mark present`
    - tapping a correction shows the shared pending label `Will save as present`
- Docs updated:
  - `Structure/requirements/08-session-history-manual-edits.md`
  - `Structure/requirements/09-reports-exports.md`
  - `Structure/architecture/08-session-history-manual-edits.md`
  - `Structure/architecture/09-reports-exports.md`
- Commands run:
  - `pnpm exec biome check --write packages/domain/src/attendance-edit.ts packages/domain/src/attendance-edit.test.ts packages/domain/src/index.test.ts apps/mobile/src/teacher-operational.ts apps/mobile/src/teacher-operational.test.ts apps/mobile/src/teacher-foundation.tsx apps/web/src/teacher-review-workflows.ts apps/web/src/teacher-review-workflows.test.ts apps/web/src/teacher-workflows-client.tsx apps/api/src/modules/attendance/attendance-history.service.ts apps/api/src/modules/attendance/attendance-history.integration.test.ts apps/api/src/modules/reports/reports.integration.test.ts`
  - `pnpm --filter @attendease/domain test -- src/attendance-edit.test.ts src/index.test.ts`
  - `pnpm --filter @attendease/mobile test -- src/teacher-operational.test.ts`
  - `pnpm --filter @attendease/web test -- src/teacher-review-workflows.test.ts`
  - `pnpm --filter @attendease/domain typecheck`
  - `pnpm --filter @attendease/mobile typecheck`
  - `pnpm --filter @attendease/api typecheck`
  - `POSTGRES_PORT=55432 pnpm runtime:check`
  - `POSTGRES_PORT=55432 pnpm test:api:integration -- src/modules/attendance/attendance-history.integration.test.ts`
  - `POSTGRES_PORT=55432 pnpm test:api:integration -- src/modules/reports/reports.integration.test.ts`
  - `pnpm --filter @attendease/web build`
  - `pnpm --filter @attendease/web typecheck`
  - `pnpm android:validate -- -d Pixel_9_Pro_XL --port 8099 --no-install`
  - `adb -s emulator-5554 shell dumpsys activity activities | rg "mResumedActivity|topResumedActivity|com.anurag203.attendease|host.exp.exponent"`
  - `adb -s emulator-5554 shell uiautomator dump /sdcard/window_dump.xml`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt38-teacher-session-detail.png`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt38-teacher-session-detail-scrolled.png`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt38-teacher-session-detail-pending.png`
  - `adb -s emulator-5554 logcat -d | rg "FATAL EXCEPTION|Maximum update depth exceeded|TypeError:|ReferenceError:|Unhandled JS Exception"`
- Blockers:
  - no prompt-38 implementation blocker
  - native Android validation for this prompt was limited to the teacher correction review flow on
    the emulator; real-device QR/Bluetooth signoff remains outside this prompt
- Exact next pickup point:
  - Prompt 39 in `Structure/phase_promptioing/20-product-reset-micro-prompts.md`

## Prompt 37 Summary

- Implemented:
  - aligned live attendance-session truth across student mobile, teacher mobile, teacher web, and
    shared API list/detail routes
  - added shared live-session discovery at:
    - `GET /sessions/live`
  - updated:
    - `apps/api/src/modules/attendance/attendance-history.service.ts`
    - `apps/api/src/modules/attendance/attendance-history.controller.ts`
    - `packages/auth/src/client.ts`
  - the backend now:
    - auto-normalizes overdue active sessions during shared history reads
    - auto-normalizes overdue active sessions during shared live-session discovery reads
    - returns one shared live-session summary shape for student and teacher clients
  - student mobile now reads live attendance candidates from the shared live-session API instead of
    deriving them from lecture-only state
  - teacher mobile now keeps home, history, active Bluetooth detail, and live roster polling
    aligned to the same session-status truth and shared poll-interval helpers
  - teacher web history polling is now aligned with the same session-status model used by the live
    QR shell, so list/detail drift is reduced while sessions are active
  - rebuilt the Docker runtime API after the live-session route change so emulator validation hit
    the actual Prompt 37 backend instead of a stale container image
- Tests or validation strengthened:
  - added:
    - `apps/mobile/src/attendance-live.ts`
    - `apps/mobile/src/attendance-live.test.ts`
  - updated:
    - `apps/api/src/modules/attendance/attendance-history.integration.test.ts`
    - `packages/auth/src/reset-client.test.ts`
    - `apps/mobile/src/student-workflow-models.test.ts`
    - `apps/mobile/src/student-attendance.test.ts`
    - `apps/mobile/src/student-query.test.ts`
    - `apps/mobile/src/teacher-query.test.ts`
    - `apps/mobile/src/teacher-operational.test.ts`
    - `apps/web/src/web-workflows.test.ts`
  - native Android validation now proves:
    - student home shows `2 attendance sessions are open`
    - student attendance hub shows `QR + GPS (1)` and `Bluetooth (1)` from shared live-session truth
    - teacher home shows `2 attendance sessions are live`
    - teacher mobile live Bluetooth detail shows the grouped live roster from shared session-student
      truth
- Docs updated:
  - `Structure/requirements/03-student-mobile-app.md`
  - `Structure/requirements/04-teacher-mobile-app.md`
  - `Structure/requirements/05-teacher-web-app.md`
  - `Structure/architecture/03-student-mobile-app.md`
  - `Structure/architecture/04-teacher-mobile-app.md`
  - `Structure/architecture/05-teacher-web-app.md`
  - `Structure/architecture/06-qr-gps-attendance.md`
  - `Structure/architecture/07-bluetooth-attendance.md`
  - `Structure/architecture/08-session-history-manual-edits.md`
- Commands run:
  - `pnpm exec biome check --write apps/api/src/modules/attendance/attendance-history.service.ts apps/api/src/modules/attendance/attendance-history.controller.ts apps/api/src/modules/attendance/attendance-history.integration.test.ts packages/auth/src/client.ts packages/auth/src/reset-client.test.ts apps/mobile/src/attendance-live.ts apps/mobile/src/attendance-live.test.ts apps/mobile/src/student-workflow-models.ts apps/mobile/src/student-workflow-models.test.ts apps/mobile/src/student-foundation.tsx apps/mobile/src/teacher-foundation.tsx apps/mobile/src/bluetooth-attendance.ts apps/web/src/web-workflows.ts apps/web/src/web-workflows.test.ts apps/web/src/teacher-workflows-client.tsx`
  - `pnpm --filter @attendease/auth test -- src/reset-client.test.ts`
  - `pnpm --filter @attendease/mobile test -- src/student-attendance.test.ts src/student-foundation.test.ts src/attendance-live.test.ts src/student-workflow-models.test.ts src/student-query.test.ts src/teacher-query.test.ts src/teacher-operational.test.ts`
  - `pnpm test:web:targeted -- src/web-workflows.test.ts`
  - `pnpm --filter @attendease/mobile typecheck`
  - `pnpm --filter @attendease/web build`
  - `pnpm --filter @attendease/web typecheck`
  - `pnpm --filter @attendease/api typecheck`
  - `POSTGRES_PORT=55432 pnpm runtime:check`
  - `POSTGRES_PORT=55432 pnpm test:api:integration -- src/modules/attendance/attendance-history.integration.test.ts`
  - `POSTGRES_PORT=55432 docker compose -f docker-compose.yml -f docker-compose.runtime.yml build api`
  - `POSTGRES_PORT=55432 docker compose -f docker-compose.yml -f docker-compose.runtime.yml up -d api`
  - `pnpm android:validate -- -d Pixel_9_Pro_XL --port 8098 --no-install`
  - `adb logcat -d | rg "FATAL EXCEPTION|Maximum update depth exceeded|AuthApiClientError|sessions/live|ReactNativeJS"`
- Blockers:
  - no prompt-37 implementation blocker
  - the only validation caveat discovered during prompt execution was a stale Docker API container
    image, which was rebuilt and then verified against the emulator
  - real-device QR and Bluetooth signoff remain outside this prompt
- Exact next pickup point:
  - Prompt 38 in `Structure/phase_promptioing/20-product-reset-micro-prompts.md`

## Prompt 36 Summary

- Implemented:
  - added the dedicated admin classroom-governance API through:
    - `apps/api/src/modules/admin/admin-classrooms.controller.ts`
    - `apps/api/src/modules/admin/admin-classroom-governance.service.ts`
    - `apps/api/src/modules/admin/admin.module.ts`
  - the admin backend now exposes:
    - `GET /admin/classrooms`
    - `GET /admin/classrooms/:classroomId`
    - `POST /admin/classrooms/:classroomId/archive`
  - admin classroom-governance detail now returns:
    - classroom scope labels
    - active/pending/blocked/dropped student counts
    - attendance-session, present, and absent totals
    - latest attendance timestamp
    - archive-readiness state
    - archive-effect and history-preserved guidance
  - admin classroom archive now requires a governance reason and is blocked while a live
    attendance session exists so archived classrooms do not cut across active attendance truth
  - shared classroom archive now accepts admin reason metadata and still preserves attendance
    history, roster history, join-code history, and audit history
  - rebuilt the admin academic-governance workspace in:
    - `apps/web/src/admin-classroom-governance.ts`
    - `apps/web/src/admin-workflows-client.tsx`
    - `apps/web/app/(admin)/admin/semesters/page.tsx`
  - `/admin/semesters` now combines:
    - semester lifecycle controls
    - classroom archive governance
  - the admin web governance lane now lets admins:
    - search classrooms
    - inspect classroom/course context
    - review archive impact before acting
    - archive safely with reason and acknowledgement
- Tests or validation strengthened:
  - added:
    - `apps/api/src/modules/admin/admin-classroom-governance.integration.test.ts`
    - `apps/web/src/admin-classroom-governance.test.ts`
  - updated:
    - `packages/contracts/src/index.test.ts`
    - `packages/auth/src/client.test.ts`
    - `apps/web/src/web-workflows.test.ts`
    - `apps/web/src/web-portal.test.ts`
  - retained proof that:
    - shared academic-management integration still passes
    - web build/typecheck still passes with the new admin governance lane
- Docs updated:
  - `Structure/requirements/05-teacher-web-app.md`
  - `Structure/architecture/05-teacher-web-app.md`
  - `Structure/architecture/11-data-rules-audit.md`
  - `Structure/architecture/13-academic-management-and-scheduling.md`
  - `Structure/architecture/15-device-trust-and-admin-controls.md`
- Commands run:
  - `pnpm exec biome check --write packages/contracts/src/academic.ts packages/contracts/src/index.test.ts packages/auth/src/client.ts packages/auth/src/client.test.ts apps/api/src/modules/academic/classrooms.service.ts apps/api/src/modules/admin/admin.module.ts apps/api/src/modules/admin/admin-classrooms.controller.ts apps/api/src/modules/admin/admin-classroom-governance.service.ts apps/api/src/modules/admin/admin-classroom-governance.integration.test.ts apps/web/src/admin-classroom-governance.ts apps/web/src/admin-classroom-governance.test.ts apps/web/src/admin-workflows-client.tsx apps/web/src/web-workflows.ts apps/web/src/web-workflows.test.ts apps/web/src/web-portal.ts apps/web/src/web-portal.test.ts apps/web/app/'(admin)'/admin/semesters/page.tsx apps/web/app/'(admin)'/admin/layout.tsx`
  - `pnpm --filter @attendease/contracts test -- src/index.test.ts`
  - `pnpm --filter @attendease/auth test -- src/client.test.ts`
  - `pnpm test:web:targeted -- src/admin-classroom-governance.test.ts src/web-workflows.test.ts src/web-portal.test.ts src/web-auth-session.test.ts`
  - `pnpm --filter @attendease/api typecheck`
  - `POSTGRES_PORT=55432 pnpm runtime:check`
  - `POSTGRES_PORT=55432 pnpm test:api:integration -- src/modules/admin/admin-classroom-governance.integration.test.ts`
  - `POSTGRES_PORT=55432 pnpm test:api:integration -- src/modules/academic/academic-management.integration.test.ts`
  - `pnpm --filter @attendease/web build`
  - `pnpm --filter @attendease/web typecheck`
  - `pnpm workspace:validate`
- Blockers:
  - no prompt-36 implementation blocker
  - real-device QR/Bluetooth validation and production-env validation remain outside the reset docs
    prompt sequence
- Exact next pickup point:
  - Prompt 37 in `Structure/phase_promptioing/20-product-reset-micro-prompts.md`

## Prompt 35 Summary

- Implemented:
  - finished the guarded admin device-recovery flow across:
    - `packages/contracts/src/devices.ts`
    - `apps/api/src/modules/admin/admin-device-support.service.ts`
    - `apps/web/src/admin-device-support.ts`
    - `apps/web/src/admin-device-support-console.tsx`
  - admin recovery list/detail payloads now include a dedicated recovery summary with:
    - current trusted phone
    - pending replacement phone
    - latest risk event
    - latest recovery action
    - API-backed recommended next step
    - strict one-device policy note
  - the admin recovery desk now shows:
    - current trusted phone
    - pending replacement phone
    - latest risk
    - latest recovery action
    - explicit next-step guidance before any change is made
  - renamed the primary recovery wording on web and docs to the reset-era product language:
    - `Deregister current phone`
    - `Approve replacement phone`
  - kept one-device enforcement strict:
    - clearing the current phone does not auto-trust a pending replacement
    - pending replacement login remains blocked until explicit approval
- Tests or validation strengthened:
  - updated:
    - `packages/contracts/src/index.test.ts`
    - `packages/auth/src/client.test.ts`
    - `apps/api/src/modules/admin/admin-device-support.integration.test.ts`
    - `apps/api/src/modules/devices/device-binding-policy.service.test.ts`
    - `apps/web/src/admin-device-support.test.ts`
    - `apps/web/src/web-portal.test.ts`
  - new proof now explicitly covers:
    - recovery-summary contract parsing
    - recovery next-step helper modeling on web
    - pending replacement staying blocked after current-phone deregistration
    - admin recovery detail showing the final recovery context
- Docs updated:
  - `Structure/architecture/15-device-trust-and-admin-controls.md`
  - `Structure/requirements/05-teacher-web-app.md`
  - `Structure/architecture/05-teacher-web-app.md`
  - `Structure/testing-strategy.md`
  - `README.md`
  - `guide.md`
  - `Structure/manual-check-quickstart.md`
- Commands run:
  - `pnpm exec biome check --write packages/contracts/src/devices.ts packages/contracts/src/index.test.ts packages/auth/src/client.test.ts apps/api/src/modules/admin/admin-device-support.service.ts apps/api/src/modules/admin/admin-device-support.integration.test.ts apps/api/src/modules/devices/device-binding-policy.service.test.ts apps/web/src/admin-device-support.ts apps/web/src/admin-device-support.test.ts apps/web/src/admin-device-support-console.tsx apps/web/src/web-portal.ts apps/web/src/web-portal.test.ts`
  - `pnpm --filter @attendease/contracts test -- src/index.test.ts`
  - `pnpm --filter @attendease/auth test -- src/client.test.ts`
  - `pnpm --filter @attendease/api test -- src/modules/devices/device-binding-policy.service.test.ts`
  - `pnpm test:web:targeted -- src/admin-device-support.test.ts src/web-portal.test.ts src/web-auth-session.test.ts`
  - `pnpm --filter @attendease/contracts typecheck`
  - `pnpm --filter @attendease/auth typecheck`
  - `pnpm --filter @attendease/api typecheck`
  - `POSTGRES_PORT=55432 pnpm runtime:check`
  - `POSTGRES_PORT=55432 pnpm test:api:integration -- src/modules/admin/admin-device-support.integration.test.ts`
  - `pnpm --filter @attendease/web build`
  - `pnpm --filter @attendease/web typecheck`
  - `pnpm workspace:validate`
- Blockers:
  - no prompt-35 implementation blocker
  - real-device QR/Bluetooth validation and production-env validation remain outside this prompt
- Exact next pickup point:
  - Prompt 36 in `Structure/phase_promptioing/20-product-reset-micro-prompts.md`

## Prompt 34 Summary

- Implemented:
  - added the dedicated admin student-governance API through:
    - `apps/api/src/modules/admin/admin-students.controller.ts`
    - `apps/api/src/modules/admin/admin-device-support.service.ts`
    - `apps/api/src/modules/admin/admin.module.ts`
  - the admin backend now exposes:
    - `GET /admin/students`
    - `GET /admin/students/:studentId`
    - `POST /admin/students/:studentId/status`
  - student support is now split cleanly from device recovery:
    - `/admin/students` returns account state, classroom context, attendance-phone context, recent
      security/admin actions, and allowed governance actions
    - `/admin/device-bindings` remains the guarded recovery surface for revoke, clear, and
      replacement-phone approval
  - admin student status changes now use audited `USER_STATUS_CHANGE` actions and revoke active
    student sessions when the new state blocks sign-in
  - account governance does not silently mutate classroom memberships or device history; those stay
    on the roster and device-recovery surfaces
  - rewired the shared auth client and support aliases in:
    - `packages/auth/src/client.ts`
    - `packages/contracts/src/devices.ts`
  - rebuilt the admin web student-support workspace in:
    - `apps/web/src/admin-workflows-client.tsx`
    - `apps/web/src/admin-student-management.ts`
    - `apps/web/app/(admin)/admin/devices/page.tsx`
  - `/admin/devices?view=support` now lets admins:
    - search students
    - inspect account state
    - inspect recent classroom context
    - inspect attendance-phone context before recovery
    - deactivate, reactivate, or archive a student account with reason + acknowledgement gating
- Tests or validation strengthened:
  - added:
    - `apps/api/src/modules/admin/admin-student-management.integration.test.ts`
    - `apps/web/src/admin-student-management.test.ts`
  - updated:
    - `packages/contracts/src/index.test.ts`
    - `packages/contracts/src/reset-contracts.test.ts`
    - `packages/auth/src/client.test.ts`
    - `packages/auth/src/reset-client.test.ts`
    - `apps/web/src/web-portal.test.ts`
  - retained proof that the existing admin device-recovery integration suite still passes unchanged
- Docs updated:
  - `Structure/requirements/02-auth-roles-enrollment.md`
  - `Structure/requirements/05-teacher-web-app.md`
  - `Structure/architecture/02-auth-roles-enrollment.md`
  - `Structure/architecture/05-teacher-web-app.md`
  - `Structure/architecture/14-classroom-communications-and-roster.md`
  - `Structure/architecture/15-device-trust-and-admin-controls.md`
  - `Structure/testing-strategy.md`
  - `README.md`
  - `guide.md`
  - `Structure/manual-check-quickstart.md`
- Commands run:
  - `pnpm exec biome check --write packages/contracts/src/devices.ts packages/contracts/src/index.test.ts packages/contracts/src/reset-contracts.test.ts packages/auth/src/client.ts packages/auth/src/client.test.ts packages/auth/src/reset-client.test.ts apps/api/src/modules/admin/admin-device-support.service.ts apps/api/src/modules/admin/admin-students.controller.ts apps/api/src/modules/admin/admin.module.ts apps/api/src/modules/admin/admin-student-management.integration.test.ts apps/web/src/admin-student-management.ts apps/web/src/admin-student-management.test.ts apps/web/src/admin-workflows-client.tsx apps/web/src/web-workflows.ts apps/web/src/web-portal.ts apps/web/src/web-portal.test.ts apps/web/app/'(admin)'/admin/devices/page.tsx`
  - `pnpm --filter @attendease/contracts test -- src/index.test.ts src/reset-contracts.test.ts`
  - `pnpm --filter @attendease/auth test -- src/client.test.ts src/reset-client.test.ts`
  - `pnpm test:web:targeted -- src/admin-student-management.test.ts src/web-portal.test.ts src/admin-device-support.test.ts src/web-auth-session.test.ts`
  - `pnpm --filter @attendease/api typecheck`
  - `pnpm --filter @attendease/web build`
  - `pnpm --filter @attendease/web typecheck`
  - `POSTGRES_PORT=55432 pnpm runtime:check`
  - `POSTGRES_PORT=55432 pnpm test:api:integration -- src/modules/admin/admin-student-management.integration.test.ts`
  - `POSTGRES_PORT=55432 pnpm test:api:integration -- src/modules/admin/admin-device-support.integration.test.ts`
- Blockers:
  - no prompt-34 implementation blocker
  - device recovery UI refinement still remains for Prompt 35, but the support/governance split is
    now real in code, tests, and docs
- Exact next pickup point:
  - Prompt 35 in `Structure/phase_promptioing/20-product-reset-micro-prompts.md`

## Prompt 32 Summary

- Implemented:
  - added shared teacher-web history and report helpers in:
    - `apps/web/src/teacher-review-workflows.ts`
  - rewired `apps/web/src/teacher-workflows-client.tsx` so teacher web session review now keeps:
    - classroom, class, section, subject, status, mode, and date filters
    - session summary cards
    - grouped `Present students` and `Absent students` sections
    - pending correction preview
    - save/reset correction actions
    - suspicious-attempt summary
    on one page
  - added a live teacher web reports workspace through:
    - `apps/web/app/(teacher)/teacher/reports/page.tsx`
    - `apps/web/src/teacher-workflows-client.tsx`
  - teacher web reports now use one shared filter scope across:
    - `GET /reports/daywise`
    - `GET /reports/subjectwise`
    - `GET /reports/students/percentages`
    and render:
    - course rollups
    - student follow-up rows
    - day-wise trend rows
  - manual attendance save on web now also invalidates teacher report query keys so reports refresh
    from corrected final attendance truth after a session edit
  - added new web query keys in:
    - `apps/web/src/web-workflows.ts`
    for:
    - day-wise reports
    - subject-wise reports
    - student-percentage reports
- Tests or validation strengthened:
  - added:
    - `apps/web/src/teacher-review-workflows.test.ts`
  - updated:
    - `apps/web/src/web-workflows.test.ts`
    - `apps/web/src/web-portal.test.ts`
- Docs updated:
  - `Structure/requirements/05-teacher-web-app.md`
  - `Structure/requirements/08-session-history-manual-edits.md`
  - `Structure/requirements/09-reports-exports.md`
  - `Structure/architecture/05-teacher-web-app.md`
  - `Structure/architecture/08-session-history-manual-edits.md`
  - `Structure/architecture/09-reports-exports.md`
  - `Structure/testing-strategy.md`
  - `README.md`
  - `guide.md`
  - `Structure/manual-check-quickstart.md`
- Commands run:
  - `pnpm exec biome check --write apps/web/src/teacher-review-workflows.ts apps/web/src/teacher-review-workflows.test.ts apps/web/src/teacher-workflows-client.tsx apps/web/src/web-workflows.ts apps/web/src/web-workflows.test.ts apps/web/src/web-portal.ts apps/web/src/web-portal.test.ts apps/web/app/'(teacher)'/teacher/reports/page.tsx`
  - `pnpm test:web:targeted -- src/teacher-review-workflows.test.ts src/web-workflows.test.ts src/web-portal.test.ts src/web-auth-session.test.ts`
  - `pnpm --filter @attendease/web build`
  - `pnpm --filter @attendease/web typecheck`
  - `pnpm workspace:validate`
- Blockers:
  - no prompt-32 implementation blocker
  - `pnpm --filter @attendease/web typecheck` still depends on `.next/types`, so it was rerun after
    the Next build in this prompt
- Exact next pickup point:
  - Prompt 33 in `Structure/phase_promptioing/20-product-reset-micro-prompts.md`

## Prompt 31 Summary

- Implemented:
  - redesigned the teacher-web live QR screen in:
    - `apps/web/src/qr-session-shell.tsx`
  - the active-session route now loads:
    - session detail through `GET /sessions/:sessionId`
    - live student rows through `GET /sessions/:sessionId/students`
  - the live teacher control view now keeps these items visible together:
    - large rolling QR
    - continuously visible timer
    - present/absent summary
    - live marked-student list
    - clear end-session control
  - the projector route now uses the same live shell with a cleaner room-facing layout focused on:
    - large QR
    - timer
    - marked-count summary
  - rewired:
    - `apps/web/app/(teacher)/teacher/sessions/active/[sessionId]/page.tsx`
    - `apps/web/app/(teacher)/teacher/sessions/active/[sessionId]/projector/page.tsx`
    so both now mount the live QR shell directly instead of wrapping it inside generic session-history page framing
  - updated the shared QR live models in:
    - `apps/web/src/web-workflows.ts`
    so teacher web now uses:
    - a fixed 2-second live refresh cadence while active
    - richer live-session labels
    - explicit live roster buckets for marked and absent students
- Tests or validation strengthened:
  - updated `apps/web/src/web-workflows.test.ts` for:
    - 2-second QR refresh cadence
    - refreshed live-session model labels
    - live roster bucket modeling for marked and absent student rows
- Commands run:
  - `pnpm exec biome check --write apps/web/src/qr-session-shell.tsx apps/web/src/web-workflows.ts apps/web/src/web-workflows.test.ts apps/web/app/'(teacher)'/teacher/sessions/active/'[sessionId]'/page.tsx apps/web/app/'(teacher)'/teacher/sessions/active/'[sessionId]'/projector/page.tsx`
  - `pnpm test:web:targeted -- src/web-workflows.test.ts src/web-portal.test.ts src/web-auth-session.test.ts`
  - `pnpm --filter @attendease/web typecheck`
  - `pnpm --filter @attendease/web build`
  - `pnpm workspace:validate`
- Blockers:
  - no prompt-31 implementation blocker
  - the live QR screen now refreshes every 2 seconds on web, but the realtime/websocket transport is still intentionally deferred and remains a later architecture step
- Exact next pickup point:
  - Prompt 32 in `Structure/phase_promptioing/20-product-reset-micro-prompts.md`

## Prompt 30 Summary

- Implemented:
  - added the dedicated teacher-web QR setup route:
    - `apps/web/app/(teacher)/teacher/sessions/start/page.tsx`
  - added shared QR setup helpers in:
    - `apps/web/src/teacher-qr-session-management.ts`
  - extended `apps/web/src/web-workflows.ts` with:
    - `teacherWorkflowRoutes.sessionStart`
  - extended `apps/web/src/web-portal.ts` so teacher dashboard, session history, and classroom
    detail page models now point teachers toward the dedicated QR setup route instead of burying
    launch inside classroom detail
  - rewired `apps/web/src/teacher-workflows-client.tsx` so:
    - QR + GPS launch now happens from the dedicated setup workspace
    - setup asks only for:
      - classroom
      - session duration
      - allowed distance
      - teacher browser location
    - session start is blocked until browser geolocation succeeds
    - classroom detail now provides a short handoff into
      `/teacher/sessions/start?classroomId=...`
    - the raw anchor fields and lecture-heavy setup block were removed from the classroom-detail
      workspace
- Tests or validation strengthened:
  - added `apps/web/src/teacher-qr-session-management.test.ts` for:
    - QR-ready classroom filtering
    - draft initialization and classroom switching
    - setup precondition blocking
    - create-payload shaping
  - updated `apps/web/src/web-workflows.test.ts` for:
    - the new `teacherWorkflowRoutes.sessionStart` route
  - updated `apps/web/src/web-portal.test.ts` for:
    - teacher dashboard QR setup routing
    - session-start page model coverage
    - classroom-detail QR handoff coverage
- Commands run:
  - `pnpm exec biome check --write apps/web/src/teacher-qr-session-management.ts apps/web/src/teacher-qr-session-management.test.ts apps/web/src/teacher-workflows-client.tsx apps/web/src/web-workflows.ts apps/web/src/web-workflows.test.ts apps/web/src/web-portal.ts apps/web/src/web-portal.test.ts apps/web/app/'(teacher)'/teacher/sessions/start/page.tsx`
  - `pnpm test:web:targeted -- src/teacher-qr-session-management.test.ts src/web-workflows.test.ts src/web-portal.test.ts src/web-auth-session.test.ts`
  - `pnpm --filter @attendease/web typecheck`
  - `pnpm --filter @attendease/web build`
  - `pnpm workspace:validate`
- Blockers:
  - no prompt-30 implementation blocker
  - QR session start now depends on teacher browser geolocation permission by design; projector/live
    session behavior continues on the existing active-session routes after setup
  - `biome check --write` is still a no-op for the Markdown-only docs in this repo, so the prompt
    relied on direct doc edits plus workspace validation for the requirement/architecture/README/guide/context sync
- Exact next pickup point:
  - Prompt 31 in `Structure/phase_promptioing/20-product-reset-micro-prompts.md`

## Prompt 29 Summary

- Implemented:
  - added shared teacher-web roster helpers in:
    - `apps/web/src/teacher-roster-management.ts`
  - rewired `apps/web/src/teacher-workflows-client.tsx` so teacher roster management now uses the
    reset-era classroom-student contract instead of the older table/status-button flow:
    - the roster page now keeps course context visible with:
      - classroom title
      - course code
      - teaching-scope summary
      - attendance mode
    - manual add now uses one lookup field for:
      - student email
      - student identifier
    - roster filters now use membership-state language on the same page as add-student
    - roster cards now show:
      - student name
      - identifier + email context
      - membership state
      - membership source
      - attendance-paused hint when applicable
      - API-driven actions for:
        - `Activate`
        - `Mark Pending`
        - `Block`
        - `Remove`
    - explicit remove now uses `removeClassroomRosterMember(...)` instead of pretending removal is
      just another generic status toggle
  - added a dedicated teacher-web roster page model in:
    - `apps/web/src/web-portal.ts`
  - updated the roster route wrapper in:
    - `apps/web/app/(teacher)/teacher/classrooms/[classroomId]/roster/page.tsx`
    so the page now uses roster-specific framing instead of the generic classroom-detail model
- Tests or validation strengthened:
  - added `apps/web/src/teacher-roster-management.test.ts` for:
    - filter shaping
    - add-student lookup shaping
    - student identity text
    - result-summary copy
    - API-driven roster action modeling
  - updated `apps/web/src/web-portal.test.ts` for:
    - teacher roster page-model actions
    - teacher roster page-model columns
- Commands run:
  - `pnpm exec biome check --write apps/web/src/teacher-roster-management.ts apps/web/src/teacher-roster-management.test.ts apps/web/src/teacher-workflows-client.tsx apps/web/src/web-portal.ts apps/web/src/web-portal.test.ts apps/web/app/'(teacher)'/teacher/classrooms/'[classroomId]'/roster/page.tsx Structure/requirements/05-teacher-web-app.md Structure/architecture/05-teacher-web-app.md Structure/architecture/14-classroom-communications-and-roster.md Structure/testing-strategy.md`
  - `pnpm test:web:targeted -- src/teacher-roster-management.test.ts src/web-portal.test.ts src/web-auth-session.test.ts`
  - `pnpm --filter @attendease/web build`
  - `pnpm --filter @attendease/web typecheck`
  - `pnpm workspace:validate`
- Blockers:
  - no prompt-29 implementation blocker
  - roster import, schedule, and stream remain separate classroom subroutes by design, but roster
    add/edit/remove work no longer depends on the older table-first web flow
- Exact next pickup point:
  - Prompt 30 in `Structure/phase_promptioing/20-product-reset-micro-prompts.md`

## Prompt 28 Summary

- Implemented:
  - added shared teacher-web classroom management helpers in:
    - `apps/web/src/teacher-classroom-management.ts`
  - rewired `apps/web/src/teacher-workflows-client.tsx` so teacher classroom management now behaves
    like one course workspace instead of separate route islands:
    - classroom list now renders management cards with:
      - `courseCode`
      - `classroomTitle`
      - teaching-scope summary
      - attendance-mode label
      - join-code label
      - direct actions for course, roster, schedule, updates, and QR tools
    - classroom create now loads allowed teaching scopes from teacher assignments and asks only
      for:
      - teaching scope
      - classroom title
      - course code
      - attendance defaults
    - classroom detail now focuses on:
      - course settings
      - join-code reset
      - archive action
      - QR launch
      - nearby classroom links
  - updated `apps/web/src/web-workflows.ts` usage and teacher route labels so classroom links now
    read:
    - `Course`
    - `Roster`
    - `Imports`
    - `Schedule`
    - `Updates`
    - `Class Sessions`
  - updated `apps/web/src/web-portal.ts` classroom page models so the teacher classroom shell now
    talks about course info, teaching scope, attendance mode, join code, QR launch, and classroom
    actions instead of placeholder-style columns
  - removed a duplicated stale classroom-detail block left behind by the first large patch in
    `apps/web/src/teacher-workflows-client.tsx`
- Tests or validation strengthened:
  - added `apps/web/src/teacher-classroom-management.test.ts` for:
    - teaching-scope option shaping
    - classroom create request shaping
    - classroom edit request shaping
    - classroom list-card modeling
  - updated `apps/web/src/web-workflows.test.ts` for:
    - `teacherAssignments` query keys
    - reset-era teacher classroom link labels
  - updated `apps/web/src/web-portal.test.ts` for:
    - classroom page-model actions
    - classroom page-model columns
    - classroom detail metrics
- Commands run:
  - `pnpm exec biome check --write apps/web/src/teacher-classroom-management.ts apps/web/src/teacher-classroom-management.test.ts apps/web/src/teacher-workflows-client.tsx apps/web/src/web-workflows.test.ts apps/web/src/web-portal.ts apps/web/src/web-portal.test.ts Structure/requirements/05-teacher-web-app.md Structure/architecture/05-teacher-web-app.md Structure/architecture/13-academic-management-and-scheduling.md Structure/testing-strategy.md`
  - `pnpm test:web:targeted -- src/teacher-classroom-management.test.ts src/web-workflows.test.ts src/web-portal.test.ts src/web-auth-session.test.ts`
  - `pnpm --filter @attendease/web build`
  - `pnpm --filter @attendease/web typecheck`
  - `pnpm workspace:validate`
- Blockers:
  - no prompt-28 implementation blocker
  - teacher-web roster, schedule, stream, and import routes remain separate route pages by design,
    but classroom create/list/detail no longer behave like disconnected utilities
- Exact next pickup point:
  - Prompt 29 in `Structure/phase_promptioing/20-product-reset-micro-prompts.md`

## Prompt 27 Summary

- Implemented:
  - redesigned the shared teacher-web chrome in:
    - `apps/web/src/web-shell.tsx`
    - `apps/web/src/web-nav.tsx`
    - `apps/web/src/web-portal.ts`
  - simplified the teacher left rail so it now uses:
    - a compact teacher label and portal description
    - a smaller signed-in account summary
    - active-route highlighting on navigation items
    - shorter, action-first navigation descriptions
  - redesigned the teacher dashboard page model so it now focuses on:
    - classrooms
    - QR attendance launch from classroom context
    - session history
    - reports
    - follow-up routes for exports and analytics
  - removed placeholder-heavy dashboard table/chart framing by replacing it with spotlight route sections inside the dashboard model
  - cleaned up teacher-facing wrapper copy in:
    - `apps/web/app/(teacher)/teacher/classrooms/page.tsx`
    - `apps/web/app/(teacher)/teacher/classrooms/new/page.tsx`
    - `apps/web/app/(teacher)/teacher/analytics/page.tsx`
    - `apps/web/app/(teacher)/teacher/email-automation/page.tsx`
    - `apps/web/app/(teacher)/teacher/exports/page.tsx`
    - `apps/web/app/(teacher)/teacher/sessions/active/[sessionId]/page.tsx`
    - `apps/web/app/(teacher)/teacher/sessions/active/[sessionId]/projector/page.tsx`
  - updated teacher-web reset docs in:
    - `Structure/requirements/05-teacher-web-app.md`
    - `Structure/architecture/05-teacher-web-app.md`
- Tests or validation strengthened:
  - updated `apps/web/src/web-portal.test.ts` to cover:
    - teacher dashboard spotlight route coverage
    - portal-nav active-route matching
    - copy validation for the new dashboard spotlight sections
  - reran:
    - `apps/web/src/web-workflows.test.ts`
    - `apps/web/src/web-auth-session.test.ts`
- Commands run:
  - `pnpm exec biome check --write apps/web/src/web-portal.ts apps/web/src/web-portal.test.ts apps/web/src/web-shell.tsx apps/web/src/web-nav.tsx apps/web/app/'(teacher)'/teacher/layout.tsx apps/web/app/'(teacher)'/teacher/classrooms/page.tsx apps/web/app/'(teacher)'/teacher/classrooms/new/page.tsx apps/web/app/'(teacher)'/teacher/analytics/page.tsx apps/web/app/'(teacher)'/teacher/email-automation/page.tsx apps/web/app/'(teacher)'/teacher/exports/page.tsx apps/web/app/'(teacher)'/teacher/sessions/active/'[sessionId]'/page.tsx apps/web/app/'(teacher)'/teacher/sessions/active/'[sessionId]'/projector/page.tsx Structure/requirements/05-teacher-web-app.md Structure/architecture/05-teacher-web-app.md`
  - `pnpm test:web:targeted -- src/web-portal.test.ts src/web-workflows.test.ts src/web-auth-session.test.ts`
  - `pnpm --filter @attendease/web typecheck`
  - `pnpm --filter @attendease/web build`
  - `pnpm workspace:validate`
- Blockers:
  - no prompt-27 implementation blocker
  - teacher web classroom CRUD still remains the next dedicated reset step and is not fully redesigned by this shell/dashboard prompt
- Exact next pickup point:
  - Prompt 28 in `Structure/phase_promptioing/20-product-reset-micro-prompts.md`

## Prompt 26 Summary

- Implemented:
  - redesigned teacher-mobile history and report review surfaces in
    `apps/mobile/src/teacher-foundation.tsx`
  - extended `apps/mobile/src/teacher-operational.ts` with:
    - a session-detail overview model for present, absent, attendance, and correction status
    - richer report row models with attendance labels, follow-up labels, freshness labels, and tone hints
    - correction-oriented row action labels (`Move To Present`, `Move To Absent`)
  - extended `apps/mobile/src/teacher-view-state.ts` with:
    - teacher session-history status states
    - teacher reports status states
  - redesigned teacher session history so it now shows:
    - a top status banner
    - an `At A Glance` summary
    - clearer `Live Now` and `Recently Saved` cards
    - correction-window messaging before the teacher opens a row
  - redesigned teacher session detail so it now shows:
    - a cleaner summary card with present, absent, attendance percentage, and correction state
    - clearer timing and final-truth copy
    - present/absent section subtitles that explain review versus correction context
  - redesigned teacher reports so it now shows:
    - a top follow-up/status banner
    - a denser overview card
    - card-based subject review rows
    - card-based student follow-up rows
    - a day-wise trend section with export entry points kept nearby
  - synced the reset docs in:
    - `Structure/requirements/04-teacher-mobile-app.md`
    - `Structure/requirements/09-reports-exports.md`
    - `Structure/architecture/04-teacher-mobile-app.md`
    - `Structure/architecture/09-reports-exports.md`
- Tests or validation strengthened:
  - `apps/mobile/src/teacher-operational.test.ts`
  - `apps/mobile/src/teacher-view-state.test.ts`
  - reran targeted teacher-mobile support coverage through:
    - `apps/mobile/src/teacher-models.test.ts`
    - `apps/mobile/src/teacher-routes.test.ts`
    - `apps/mobile/src/teacher-query.test.ts`
- Commands run:
  - `pnpm exec biome check --write apps/mobile/src/teacher-operational.ts apps/mobile/src/teacher-operational.test.ts apps/mobile/src/teacher-view-state.ts apps/mobile/src/teacher-view-state.test.ts apps/mobile/src/teacher-foundation.tsx Structure/requirements/04-teacher-mobile-app.md Structure/requirements/09-reports-exports.md Structure/architecture/04-teacher-mobile-app.md Structure/architecture/09-reports-exports.md`
  - `pnpm test:mobile:targeted -- src/teacher-operational.test.ts src/teacher-view-state.test.ts src/teacher-models.test.ts src/teacher-routes.test.ts src/teacher-query.test.ts`
  - `pnpm --filter @attendease/mobile typecheck`
  - `POSTGRES_PORT=55432 pnpm runtime:check`
  - `pnpm workspace:validate`
  - `adb devices -l`
  - `adb -s emulator-5554 logcat -c`
  - `pnpm android:validate -- -d Pixel_9_Pro_XL --port 8098 --no-install`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt26-current.png`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt26-teacher-signin.png`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt26-teacher-home.png`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt26-session-history.png`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt26-teacher-reports.png`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt26-teacher-reports-detail.png`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt26-session-detail.png`
  - `adb -s emulator-5554 shell uiautomator dump /sdcard/window_dump.xml`
  - `adb -s emulator-5554 logcat -d | rg "ReactNativeJS|FATAL EXCEPTION|TypeError|AuthApiClientError|Maximum update depth exceeded" -n | tail -n 80`
- Android validation evidence:
  - `Structure/artifacts/android-prompt26-teacher-signin.png`
  - `Structure/artifacts/android-prompt26-teacher-home.png`
  - `Structure/artifacts/android-prompt26-session-history.png`
  - `Structure/artifacts/android-prompt26-teacher-reports.png`
  - `Structure/artifacts/android-prompt26-teacher-reports-detail.png`
  - `Structure/artifacts/android-prompt26-session-detail.png`
  - emulator proof now covers:
    - teacher sign-in into the teacher route tree
    - a teacher history screen with status banner, at-a-glance cards, and review-ready saved-session rows
    - a teacher reports screen with follow-up status banner, summary copy, subject cards, and student follow-up cards
    - a teacher session-detail screen with present/absent, attendance percentage, correction status, and clearer review copy
- Blockers:
  - no prompt-26 implementation blocker
  - real nearby BLE advertising and student discovery still remain real-device-only and are not changed by this reporting/history prompt
- Exact next pickup point:
  - Prompt 27 in `Structure/phase_promptioing/20-product-reset-micro-prompts.md`

## Prompt 25 Summary

- Implemented:
  - redesigned the teacher-mobile live attendance and post-session correction flow in
    `apps/mobile/src/teacher-foundation.tsx`
  - extended `apps/mobile/src/teacher-operational.ts` with:
    - grouped present/absent session-roster models
    - row-level action labels for `Mark Present` and `Mark Absent`
    - live/open/locked session-detail status messaging
  - extended the active teacher Bluetooth route so it now shows a live student list with:
    - marked-present students
    - still-absent students
    - one-tap refresh
  - redesigned teacher session history so live sessions are separated from saved sessions
  - redesigned teacher session detail so it now shows:
    - a live/read-only status banner while attendance is still active
    - grouped `Present Students` and `Absent Students` sections
    - explicit correction state and save/reset actions after session end
    - one-tap correction toggles that move a student between present and absent draft groups
- Tests or validation strengthened:
  - `apps/mobile/src/teacher-operational.test.ts`
  - reran targeted teacher-mobile session coverage through:
    - `apps/mobile/src/teacher-routes.test.ts`
    - `apps/mobile/src/teacher-models.test.ts`
    - `apps/mobile/src/teacher-view-state.test.ts`
  - reran the shared attendance-history integration suite to prove:
    - live sessions still reject manual edits
    - ended sessions still accept manual add/remove updates transactionally
- Commands run:
  - `pnpm exec biome check --write apps/mobile/src/teacher-operational.ts apps/mobile/src/teacher-operational.test.ts apps/mobile/src/teacher-foundation.tsx`
  - `pnpm test:mobile:targeted -- src/teacher-operational.test.ts src/teacher-routes.test.ts src/teacher-models.test.ts src/teacher-view-state.test.ts`
  - `pnpm --filter @attendease/mobile typecheck`
  - `POSTGRES_PORT=55432 pnpm test:api:integration -- src/modules/attendance/attendance-history.integration.test.ts`
  - `POSTGRES_PORT=55432 pnpm runtime:check`
  - `pnpm android:validate -- -d Pixel_9_Pro_XL --port 8097 --no-install`
  - `pnpm exec tsx <<'EOF' ... listAttendanceSessions ... EOF`
  - `pnpm exec tsx <<'EOF' ... createBluetoothAttendanceSession ... markBluetoothAttendance ... EOF`
  - `pnpm exec tsx <<'EOF' ... endAttendanceSession ... EOF`
  - `pnpm exec tsx <<'EOF' ... getAttendanceSessionDetail ... EOF`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt25-teacher-signin.png`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt25-teacher-home.png`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt25-session-history.png`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt25-live-session-detail.png`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt25-live-session-roster.png`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt25-ended-session-detail.png`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt25-correction-pending.png`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt25-correction-save-ready.png`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt25-correction-saved.png`
  - `adb -s emulator-5554 logcat -d | rg "ReactNativeJS|FATAL EXCEPTION|Maximum update depth exceeded|AuthApiClientError|TypeError|Bluetooth" -n | tail -n 120`
  - `pnpm workspace:validate`
- Android validation evidence:
  - `Structure/artifacts/android-prompt25-teacher-signin.png`
  - `Structure/artifacts/android-prompt25-teacher-home.png`
  - `Structure/artifacts/android-prompt25-session-history.png`
  - `Structure/artifacts/android-prompt25-live-session-detail.png`
  - `Structure/artifacts/android-prompt25-live-session-roster.png`
  - `Structure/artifacts/android-prompt25-ended-session-detail.png`
  - `Structure/artifacts/android-prompt25-correction-pending.png`
  - `Structure/artifacts/android-prompt25-correction-save-ready.png`
  - `Structure/artifacts/android-prompt25-correction-saved.png`
  - emulator proof now covers:
    - teacher sign-in into the teacher-only route tree
    - teacher session history with separate `Live Now` and `Past Sessions` cards
    - a live Bluetooth session row showing `1 present / 3 absent`
    - live session detail showing active-session lock messaging plus grouped present/absent lists
    - ended session detail showing editable grouped lists with `Mark Present` / `Mark Absent`
    - a pending correction state with `Save Attendance Changes` enabled
    - a saved correction state with refreshed totals and success feedback
- Blockers:
  - no prompt-25 implementation blocker
  - real nearby BLE discovery and multi-device proximity signoff still remain real-device-only and
    are not satisfied by emulator validation
- Exact next pickup point:
  - Prompt 26 in `Structure/phase_promptioing/20-product-reset-micro-prompts.md`

## Prompt 24 Summary

- Implemented:
  - finished the reset-track teacher mobile Bluetooth session setup and control flow in
    `apps/mobile/src/teacher-foundation.tsx`
  - extended `apps/mobile/src/teacher-operational.ts` with:
    - teacher Bluetooth setup-status models for create-route guidance
    - teacher Bluetooth active-status models for live, stopped, permission, failed, and ended
      states
    - shorter teacher-facing control and end-session labels
  - extended `apps/mobile/src/bluetooth-attendance.ts` so teacher advertiser failures now normalize
    into permission-aware or unsupported recovery states instead of surfacing one generic failure
  - redesigned the teacher Bluetooth create route around:
    - a setup banner
    - classroom-scoped selection cards
    - duration and class-session context
    - one clear `Start Bluetooth Attendance` action
  - redesigned the active teacher Bluetooth route around:
    - a live status banner
    - a `Broadcast Controls` card
    - a dedicated `End Session` card
    - a post-session navigation card
  - updated session closeout so successful end now routes directly into teacher session detail
- Tests or validation strengthened:
  - `apps/mobile/src/bluetooth-attendance.test.ts`
  - `apps/mobile/src/teacher-operational.test.ts`
  - reran targeted teacher-mobile Bluetooth coverage through:
    - `apps/mobile/src/teacher-routes.test.ts`
    - `apps/mobile/src/teacher-models.test.ts`
    - `apps/mobile/src/teacher-view-state.test.ts`
- Commands run:
  - `pnpm exec biome check --write apps/mobile/src/bluetooth-attendance.ts apps/mobile/src/bluetooth-attendance.test.ts apps/mobile/src/teacher-operational.ts apps/mobile/src/teacher-operational.test.ts apps/mobile/src/teacher-foundation.tsx`
  - `pnpm test:mobile:targeted -- src/bluetooth-attendance.test.ts src/teacher-operational.test.ts src/teacher-routes.test.ts src/teacher-models.test.ts src/teacher-view-state.test.ts`
  - `pnpm --filter @attendease/mobile typecheck`
  - `POSTGRES_PORT=55432 pnpm runtime:check`
  - `pnpm android:validate -- -d Pixel_9_Pro_XL --port 8096 --no-install`
  - `adb -s emulator-5554 logcat -c`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt24-teacher-signin.png`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt24-teacher-home.png`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt24-bluetooth-create.png`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt24-bluetooth-create-selection.png`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt24-bluetooth-active.png`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt24-bluetooth-active-end.png`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt24-bluetooth-ended-detail.png`
  - `adb -s emulator-5554 logcat -d | rg "ReactNativeJS|FATAL EXCEPTION|Maximum update depth exceeded|AuthApiClientError|TypeError|Bluetooth" -n | tail -n 80`
  - `pnpm workspace:validate`
- Android validation evidence:
  - `Structure/artifacts/android-prompt24-teacher-signin.png`
  - `Structure/artifacts/android-prompt24-teacher-home.png`
  - `Structure/artifacts/android-prompt24-bluetooth-create.png`
  - `Structure/artifacts/android-prompt24-bluetooth-create-selection.png`
  - `Structure/artifacts/android-prompt24-bluetooth-active.png`
  - `Structure/artifacts/android-prompt24-bluetooth-active-end.png`
  - `Structure/artifacts/android-prompt24-bluetooth-ended-detail.png`
  - emulator proof now covers:
    - teacher sign-in into the teacher-only route tree
    - classroom selection from the Bluetooth setup route
    - Bluetooth session creation and navigation into the active control route
    - clear advertiser recovery UI on the active route
    - ending Bluetooth attendance and landing in saved session detail
- Blockers:
  - no prompt-24 implementation blocker
  - real nearby BLE advertising and student detection still remain real-device-only and are not
    satisfied by emulator validation
- Exact next pickup point:
  - Prompt 25 in `Structure/phase_promptioing/20-product-reset-micro-prompts.md`

## Prompt 18 Summary

- Implemented:
  - redesigned the student QR attendance route in `apps/mobile/src/student-foundation.tsx` into a short four-step flow:
    - choose session
    - scan QR
    - confirm location
    - mark attendance
  - added QR-specific scan-state modeling in `apps/mobile/src/student-attendance.ts` through `buildStudentQrScanBanner()`
  - replaced scaffold-style QR copy with product-facing permission, location, success, and failure banners in `apps/mobile/src/student-attendance.ts`
  - tightened result-state language so the student now sees concise states for:
    - attendance marked
    - no attendance session open
    - QR payload required
    - location needed
    - session closed
    - expired QR
    - invalid QR
    - low-accuracy GPS
    - out-of-range GPS
    - duplicate attendance
  - kept camera use optional by preserving manual QR entry as the fallback when camera access is denied or unavailable
  - kept trusted-device, QR signature, GPS distance, and GPS accuracy validation behavior unchanged
- Tests or validation strengthened:
  - `apps/mobile/src/student-attendance.test.ts`
  - reran targeted QR-related mobile tests through:
    - `apps/mobile/src/student-foundation.test.ts`
    - `apps/mobile/src/student-view-state.test.ts`
- Commands run:
  - `pnpm exec biome check --write apps/mobile/src/student-attendance.ts apps/mobile/src/student-attendance.test.ts apps/mobile/src/student-foundation.tsx Structure/requirements/03-student-mobile-app.md Structure/requirements/06-qr-gps-attendance.md Structure/architecture/03-student-mobile-app.md Structure/architecture/06-qr-gps-attendance.md README.md guide.md Structure/manual-check-quickstart.md Structure/context.md`
  - `pnpm test:mobile:targeted -- src/student-attendance.test.ts src/student-foundation.test.ts src/student-view-state.test.ts`
  - `pnpm --filter @attendease/mobile typecheck`
  - `POSTGRES_PORT=55432 pnpm runtime:check`
  - `pnpm android:validate -- -d Pixel_9_Pro_XL --port 8092 --no-install`
  - `adb -s emulator-5554 emu geo fix 77.2090 28.6139`
  - `adb -s emulator-5554 shell pm revoke com.anurag203.attendease android.permission.CAMERA`
  - `adb -s emulator-5554 shell pm revoke com.anurag203.attendease android.permission.ACCESS_FINE_LOCATION`
  - `adb -s emulator-5554 shell pm revoke com.anurag203.attendease android.permission.ACCESS_COARSE_LOCATION`
  - `adb -s emulator-5554 shell pm grant com.anurag203.attendease android.permission.ACCESS_FINE_LOCATION`
  - `adb -s emulator-5554 shell pm grant com.anurag203.attendease android.permission.ACCESS_COARSE_LOCATION`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt18-qr-route-initial.png`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt18-qr-camera-permission.png`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt18-qr-camera-denied.png`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt18-qr-location-permission.png`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt18-qr-location-denied.png`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt18-qr-location-ready.png`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt18-qr-camera-ready.png`
  - `adb -s emulator-5554 logcat -d | rg "ReactNativeJS|FATAL EXCEPTION|Maximum update depth exceeded" -n | tail -n 40`
  - `pnpm workspace:validate`
- Android validation evidence:
  - `Structure/artifacts/android-prompt18-qr-route-initial.png`
  - `Structure/artifacts/android-prompt18-qr-camera-permission.png`
  - `Structure/artifacts/android-prompt18-qr-camera-denied.png`
  - `Structure/artifacts/android-prompt18-qr-location-permission.png`
  - `Structure/artifacts/android-prompt18-qr-location-denied.png`
  - `Structure/artifacts/android-prompt18-qr-location-ready.png`
  - `Structure/artifacts/android-prompt18-qr-camera-ready.png`
  - the emulator route sweep confirmed:
    - the QR route now opens with the simplified four-step structure
    - camera permission is requested only when `Use camera` is pressed
    - denied camera access falls back to a clear manual-entry path
    - location permission is requested only at the location step
    - denied and ready location states are both explicit
    - the positive camera path now opens a live scanner preview without fresh fatal logcat errors
- Blockers:
  - no prompt-18 implementation blocker
  - emulator validation covered camera permission, location permission, and positive scanner-preview behavior, but full real-phone QR signoff still remains outside the reset prompt
- Exact next pickup point:
  - Prompt 19 in `Structure/phase_promptioing/20-product-reset-micro-prompts.md`

## Prompt 23 Summary

- Implemented:
  - redesigned the teacher-mobile roster route in `apps/mobile/src/teacher-foundation.tsx` so normal classroom roster work now stays inside one phone-first screen
  - added shared teacher-roster workflow helpers in `apps/mobile/src/teacher-roster-management.ts` for:
    - roster filter shaping
    - manual add request shaping
    - member identity text
    - visible member actions
    - result summaries
  - extended `apps/mobile/src/teacher-query.ts` so classroom-roster query keys now include normalized roster filters and search state
  - extended `apps/mobile/src/teacher-view-state.ts` with a dedicated teacher-roster status model for loading, empty, filtered-empty, and ready states
  - rebuilt the teacher roster screen around:
    - a course-context summary card
    - a short `Add Student` form
    - a `Find Students` search/filter card
    - direct `Mark Active`, `Mark Pending`, `Block`, and `Remove` row actions when the API allows them
    - a secondary `Bulk Import` area that no longer competes with normal roster work
- Tests or validation strengthened:
  - `apps/mobile/src/teacher-roster-management.test.ts`
  - `apps/mobile/src/teacher-view-state.test.ts`
  - `apps/mobile/src/teacher-query.test.ts`
  - reran targeted teacher-mobile roster coverage through:
    - `apps/mobile/src/teacher-models.test.ts`
    - `apps/mobile/src/teacher-routes.test.ts`
- Commands run:
  - `pnpm exec biome check --write apps/mobile/src/teacher-roster-management.ts apps/mobile/src/teacher-roster-management.test.ts apps/mobile/src/teacher-view-state.ts apps/mobile/src/teacher-view-state.test.ts apps/mobile/src/teacher-query.ts apps/mobile/src/teacher-query.test.ts apps/mobile/src/teacher-foundation.tsx Structure/requirements/04-teacher-mobile-app.md Structure/architecture/04-teacher-mobile-app.md Structure/architecture/14-classroom-communications-and-roster.md Structure/testing-strategy.md README.md guide.md Structure/manual-check-quickstart.md Structure/context.md`
  - `pnpm test:mobile:targeted -- src/teacher-roster-management.test.ts src/teacher-view-state.test.ts src/teacher-query.test.ts src/teacher-models.test.ts src/teacher-routes.test.ts`
  - `pnpm --filter @attendease/mobile typecheck`
  - `POSTGRES_PORT=55432 pnpm runtime:check`
  - `pnpm android:validate -- -d Pixel_9_Pro_XL --port 8095 --no-install`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt23-teacher-signin.png`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt23-teacher-home.png`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt23-teacher-classrooms.png`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt23-teacher-roster.png`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt23-teacher-roster-filters.png`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt23-teacher-roster-search.png`
  - `adb -s emulator-5554 logcat -d | rg "ReactNativeJS|FATAL EXCEPTION|Maximum update depth exceeded|AuthApiClientError|TypeError" -n | tail -n 80`
  - `pnpm workspace:validate`
- Android validation evidence:
  - `Structure/artifacts/android-prompt23-teacher-signin.png`
  - `Structure/artifacts/android-prompt23-teacher-home.png`
  - `Structure/artifacts/android-prompt23-teacher-classrooms.png`
  - `Structure/artifacts/android-prompt23-teacher-roster.png`
  - `Structure/artifacts/android-prompt23-teacher-roster-filters.png`
  - `Structure/artifacts/android-prompt23-teacher-roster-search.png`
  - the emulator route sweep now proves:
    - teacher sign-in still lands in the teacher-only route tree
    - the classroom list still routes cleanly into roster
    - the roster screen shows course context, add-student controls, and live member-action buttons
    - the roster route can also show the search/filter surface without fresh fatal logcat errors
- Blockers:
  - no prompt-23 implementation blocker
  - Android validation still relies on root app launch plus in-app teacher navigation because direct nested teacher deep links remain less reliable in the native dev client
- Exact next pickup point:
  - Prompt 24 in `Structure/phase_promptioing/20-product-reset-micro-prompts.md`

## Prompt 19 Summary

- Implemented:
  - redesigned the student Bluetooth attendance route in `apps/mobile/src/student-foundation.tsx` around shorter product-facing step cards:
    - choose session
    - check Bluetooth
    - scan nearby teacher
    - mark attendance
  - replaced scaffold-like Bluetooth copy with product-facing scanner, detection, and submission banners from `apps/mobile/src/bluetooth-attendance.ts`
  - normalized raw Android BLE permission failures into a student-safe `Bluetooth needed` state instead of surfacing the native `SecurityException`
  - added explicit refresh actions in the student attendance hub and Bluetooth route so a teacher-started Bluetooth session can appear without restarting the student app
  - extended `useStudentAttendanceController()` so live attendance refresh now invalidates all active classroom lecture queries plus trusted-device readiness instead of only the currently selected lecture path
  - added reusable attendance-refresh messaging in `apps/mobile/src/student-view-state.ts`
- Tests or validation strengthened:
  - `apps/mobile/src/bluetooth-attendance.test.ts`
  - `apps/mobile/src/student-attendance.test.ts`
  - `apps/mobile/src/student-view-state.test.ts`
- Commands run:
  - `pnpm exec biome check --write apps/mobile/src/student-foundation.tsx apps/mobile/src/student-view-state.ts apps/mobile/src/student-view-state.test.ts`
  - `pnpm test:mobile:targeted -- src/bluetooth-attendance.test.ts src/student-attendance.test.ts src/student-view-state.test.ts`
  - `pnpm --filter @attendease/mobile typecheck`
  - `POSTGRES_PORT=55432 pnpm runtime:check`
  - `pnpm exec tsx -e 'import { createAuthApiClient } from "@attendease/auth"; import { developmentSeedIds, developmentAuthFixtures } from "@attendease/db"; void (async () => { const client = createAuthApiClient({ baseUrl: "http://127.0.0.1:4000" }); const teacher = await client.loginTeacher({ email: developmentAuthFixtures.teacher.email, password: developmentAuthFixtures.teacher.password, platform: "MOBILE", requestedRole: "TEACHER" }); const created = await client.createBluetoothAttendanceSession(teacher.tokens.accessToken, { classroomId: developmentSeedIds.courseOfferings.physics, sessionDurationMinutes: 20 }); console.log(JSON.stringify(created, null, 2)); })();'`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt19-attendance-hub-refreshed-before-tap.png`
  - `adb -s emulator-5554 shell uiautomator dump /sdcard/android-prompt19-attendance-hub-refreshed-before-tap.xml`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt19-bluetooth-live-route.png`
  - `adb -s emulator-5554 shell uiautomator dump /sdcard/android-prompt19-bluetooth-live-route.xml`
  - `adb -s emulator-5554 shell pm grant com.anurag203.attendease android.permission.BLUETOOTH_SCAN`
  - `adb -s emulator-5554 shell pm grant com.anurag203.attendease android.permission.BLUETOOTH_CONNECT`
  - `adb -s emulator-5554 shell pm grant com.anurag203.attendease android.permission.ACCESS_FINE_LOCATION`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt19-bluetooth-live-route-permissions-granted.png`
  - `adb -s emulator-5554 shell uiautomator dump /sdcard/android-prompt19-bluetooth-live-route-permissions-granted.xml`
  - `adb -s emulator-5554 logcat -d | rg "ReactNativeJS|FATAL EXCEPTION|SecurityException|BLUETOOTH_SCAN|Bluetooth" -n | tail -n 80`
  - `pnpm exec tsx -e 'import { createAuthApiClient } from "@attendease/auth"; import { developmentAuthFixtures } from "@attendease/db"; void (async () => { const client = createAuthApiClient({ baseUrl: "http://127.0.0.1:4000" }); const teacher = await client.loginTeacher({ email: developmentAuthFixtures.teacher.email, password: developmentAuthFixtures.teacher.password, platform: "MOBILE", requestedRole: "TEACHER" }); const sessions = await client.listLiveAttendanceSessions(teacher.tokens.accessToken, { mode: "BLUETOOTH" }); for (const session of sessions) { await client.endAttendanceSession(teacher.tokens.accessToken, session.id); } console.log(JSON.stringify({ endedSessionIds: sessions.map((session) => session.id) }, null, 2)); })();'`
  - `pnpm workspace:validate`
- Android validation evidence:
  - `Structure/artifacts/android-prompt19-attendance-hub-refreshed-before-tap.png`
  - `Structure/artifacts/android-prompt19-attendance-hub-refreshed-before-tap.xml`
  - `Structure/artifacts/android-prompt19-bluetooth-live-route.png`
  - `Structure/artifacts/android-prompt19-bluetooth-live-route.xml`
  - `Structure/artifacts/android-prompt19-bluetooth-live-route-permissions-granted.png`
  - `Structure/artifacts/android-prompt19-bluetooth-live-route-permissions-granted.xml`
  - emulator proof now covers:
    - the attendance hub surfacing a live `Bluetooth (1)` session after backend session creation
    - the Bluetooth route showing a real classroom session card for `Physics - Semester 6 A`
    - the Bluetooth route showing student-friendly permission guidance instead of the raw Android BLE exception
- Blockers:
  - no prompt-19 implementation blocker
  - live BLE discovery beyond session selection still remains real-device-only because the emulator BLE environment does not provide trustworthy nearby-teacher advertising or scanning behavior
- Exact next pickup point:
  - Prompt 20 in `Structure/phase_promptioing/20-product-reset-micro-prompts.md`

## Prompt 20 Summary

- Implemented:
  - replaced the student mobile history placeholder with a real student-owned attendance history flow backed by `GET /students/me/history`
  - added `StudentAttendanceHistory` contracts and the shared auth client helper in:
    - `packages/contracts/src/attendance.ts`
    - `packages/auth/src/client.ts`
  - added the live student history route in the API through:
    - `apps/api/src/modules/attendance/student-attendance-history.controller.ts`
    - `apps/api/src/modules/attendance/attendance-history.service.ts`
    - `apps/api/src/modules/attendance/attendance-history.models.ts`
    - `apps/api/src/modules/attendance/attendance.module.ts`
  - redesigned the student history screen in `apps/mobile/src/student-foundation.tsx` so it now shows:
    - a plain-language attendance insight
    - attendance percentage
    - recorded-session count
    - present count
    - absent count
    - last recorded session
    - recent course rows with present or absent outcome, attendance mode, and session detail
  - redesigned the student reports surface in `apps/mobile/src/student-foundation.tsx` so the overview and subject cards are easier to read, while keeping the final backend report APIs and subject-detail flow intact
  - tightened the student profile and device-status surfaces so they now stay focused on:
    - joined-classroom count
    - whether this phone can mark attendance
    - safe on-device name preferences
    - student-facing device help without admin-only recovery language
  - fixed a real runtime gap found during emulator validation by rebuilding the Docker API container so the live Android app could reach the new `/students/me/history` route
  - fixed a small history UX bug so the screen now shows `Loading attendance history` during the first fetch instead of incorrectly flashing `No history yet`
- Tests or validation strengthened:
  - `packages/contracts/src/index.test.ts`
  - `packages/auth/src/client.test.ts`
  - `apps/api/src/modules/attendance/attendance-history.models.test.ts`
  - `apps/api/src/modules/attendance/attendance-history.integration.test.ts`
  - `apps/mobile/src/student-view-state.test.ts`
  - `apps/mobile/src/student-workflow-models.test.ts`
  - existing student-foundation coverage was rerun through:
    - `apps/mobile/src/student-foundation.test.ts`
- Commands run:
  - `pnpm exec biome check --write packages/contracts/src/attendance.ts packages/contracts/src/index.test.ts packages/auth/src/client.ts packages/auth/src/client.test.ts apps/api/src/modules/attendance/student-attendance-history.controller.ts apps/api/src/modules/attendance/attendance-history.service.ts apps/api/src/modules/attendance/attendance-history.models.ts apps/api/src/modules/attendance/attendance.module.ts apps/api/src/modules/attendance/attendance-history.models.test.ts apps/api/src/modules/attendance/attendance-history.integration.test.ts apps/mobile/src/student-foundation.tsx apps/mobile/src/student-view-state.ts apps/mobile/src/student-view-state.test.ts apps/mobile/src/student-workflow-models.ts apps/mobile/src/student-workflow-models.test.ts Structure/requirements/03-student-mobile-app.md Structure/requirements/09-reports-exports.md Structure/architecture/03-student-mobile-app.md Structure/architecture/09-reports-exports.md Structure/context.md`
  - `pnpm --filter @attendease/contracts test -- src/index.test.ts`
  - `pnpm --filter @attendease/auth test -- src/client.test.ts`
  - `pnpm --filter @attendease/mobile test -- src/student-view-state.test.ts src/student-workflow-models.test.ts src/student-foundation.test.ts`
  - `pnpm --filter @attendease/api test -- src/modules/attendance/attendance-history.models.test.ts`
  - `pnpm --filter @attendease/mobile typecheck`
  - `pnpm --filter @attendease/api typecheck`
  - `pnpm --filter @attendease/auth typecheck`
  - `pnpm --filter @attendease/contracts typecheck`
  - `POSTGRES_PORT=55432 pnpm test:api:integration -- src/modules/attendance/attendance-history.integration.test.ts src/modules/reports/reports.integration.test.ts`
  - `POSTGRES_PORT=55432 docker compose -f docker-compose.yml -f docker-compose.runtime.yml build api`
  - `POSTGRES_PORT=55432 docker compose -f docker-compose.yml -f docker-compose.runtime.yml up -d api`
  - `POSTGRES_PORT=55432 pnpm runtime:check`
  - `pnpm android:validate -- -d Pixel_9_Pro_XL --port 8092 --no-install`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt20-student-reports2.png`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt20-student-history4.png`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt20-student-profile2.png`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt20-student-device-status2.png`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt20-student-subject-report2.png`
  - `adb -s emulator-5554 logcat -d | rg "ReactNativeJS|FATAL EXCEPTION|Unhandled|Cannot GET /students/me/history|Maximum update depth" -n | tail -n 80`
  - `pnpm workspace:validate`
- Android validation evidence:
  - `Structure/artifacts/android-prompt20-student-reports2.png`
  - `Structure/artifacts/android-prompt20-student-reports2.xml`
  - `Structure/artifacts/android-prompt20-student-history4.png`
  - `Structure/artifacts/android-prompt20-student-history4.xml`
  - `Structure/artifacts/android-prompt20-student-profile2.png`
  - `Structure/artifacts/android-prompt20-student-profile2.xml`
  - `Structure/artifacts/android-prompt20-student-device-status2.png`
  - `Structure/artifacts/android-prompt20-student-device-status2.xml`
  - `Structure/artifacts/android-prompt20-student-subject-report2.png`
  - `Structure/artifacts/android-prompt20-student-subject-report2.xml`
  - emulator proof now covers:
    - report overview with backend totals, present count, absent count, tracked classrooms, and subject cards
    - subject detail with classroom-level attendance breakdown
    - attendance history with real recent present or absent rows after the API container rebuild
    - profile summary with joined-classroom count, trusted-phone state, and safe on-device profile editing
    - device-status detail with live attendance-readiness state in student-facing language
- Blockers:
  - no prompt-20 implementation blocker
  - real-phone validation for these reset surfaces still remains outside the reset prompt, but the emulator proof now covers the intended history, reports, profile, and device-status UX
- Exact next pickup point:
  - Prompt 23 in `Structure/phase_promptioing/20-product-reset-micro-prompts.md`

## Prompt 22 Summary

- Implemented:
  - redesigned teacher-mobile classroom management in `apps/mobile/src/teacher-foundation.tsx` so
    the `Classrooms` screen now acts as the main task-oriented classroom workspace instead of a
    generic card list
  - added shared classroom-management helpers in `apps/mobile/src/teacher-classroom-management.ts`
    for:
    - labeled teaching-scope options
    - create draft state
    - edit draft state
    - classroom create request shaping
    - classroom update request shaping
    - edit dirty-state detection
    - concise classroom supporting text
  - extended teacher assignment contract shaping so teacher-mobile create flows can show readable
    semester, class, section, and subject labels instead of raw IDs through:
    - `packages/contracts/src/academic.ts`
    - `apps/api/src/modules/academic/assignments.service.ts`
  - updated the teacher `Classrooms` route so it now provides:
    - a classroom-management status banner
    - a `Manage Classrooms` card
    - an inline `Create classroom` flow
    - labeled teaching-scope selection
    - direct classroom-detail routing after create
  - updated teacher classroom detail so it now provides:
    - short course summary copy
    - inline `Course Info` editing for classroom title and course code
    - permission-aware `Archive Classroom` handling
    - history-safe archive explanatory copy
  - kept the shared classroom CRUD API and permission model as the single source of truth for
    teacher-mobile create/edit/archive behavior
- Tests or validation strengthened:
  - `packages/contracts/src/index.test.ts`
  - `packages/auth/src/client.test.ts`
  - `apps/api/src/modules/academic/assignments.service.test.ts`
  - `apps/api/src/modules/auth/auth.integration.test.ts`
  - `apps/mobile/src/teacher-classroom-management.test.ts`
  - `apps/mobile/src/teacher-view-state.test.ts`
  - existing teacher-mobile route and classroom tests were rerun through:
    - `apps/mobile/src/teacher-models.test.ts`
    - `apps/mobile/src/teacher-routes.test.ts`
- Commands run:
  - `pnpm exec biome check --write packages/contracts/src/academic.ts packages/contracts/src/index.test.ts packages/auth/src/client.test.ts apps/api/src/modules/academic/assignments.service.ts apps/api/src/modules/academic/assignments.service.test.ts apps/api/src/modules/auth/auth.integration.test.ts apps/mobile/src/teacher-classroom-management.ts apps/mobile/src/teacher-classroom-management.test.ts apps/mobile/src/teacher-view-state.ts apps/mobile/src/teacher-view-state.test.ts apps/mobile/src/teacher-foundation.tsx Structure/requirements/04-teacher-mobile-app.md Structure/architecture/04-teacher-mobile-app.md Structure/architecture/13-academic-management-and-scheduling.md Structure/testing-strategy.md README.md guide.md Structure/manual-check-quickstart.md Structure/context.md`
  - `pnpm --filter @attendease/contracts test -- src/index.test.ts`
  - `pnpm --filter @attendease/auth test -- src/client.test.ts`
  - `pnpm --filter @attendease/api test -- src/modules/academic/assignments.service.test.ts`
  - `pnpm test:mobile:targeted -- src/teacher-classroom-management.test.ts src/teacher-view-state.test.ts src/teacher-models.test.ts src/teacher-routes.test.ts`
  - `pnpm --filter @attendease/mobile typecheck`
  - `pnpm --filter @attendease/api typecheck`
  - `POSTGRES_PORT=55432 pnpm test:api:integration -- src/modules/auth/auth.integration.test.ts src/modules/academic/academic-management.integration.test.ts`
  - `POSTGRES_PORT=55432 docker compose -f docker-compose.yml -f docker-compose.runtime.yml build api`
  - `POSTGRES_PORT=55432 docker compose -f docker-compose.yml -f docker-compose.runtime.yml up -d api`
  - `POSTGRES_PORT=55432 pnpm runtime:check`
  - `pnpm android:validate -- -d Pixel_9_Pro_XL --port 8094 --no-install`
  - `adb -s emulator-5554 logcat -d | rg \"ReactNativeJS|FATAL EXCEPTION|Maximum update depth exceeded|AuthApiClientError|TypeError\" -n | tail -n 80`
  - `pnpm workspace:validate`
- Android validation evidence:
  - `Structure/artifacts/android-prompt22-teacher-home.png`
  - `Structure/artifacts/android-prompt22-teacher-classrooms-refreshed.png`
  - `Structure/artifacts/android-prompt22-create-classroom-refreshed.png`
  - `Structure/artifacts/android-prompt22-classroom-detail.png`
  - `Structure/artifacts/android-prompt22-classroom-edit.png`
  - `Structure/artifacts/android-prompt22-classroom-archive.png`
  - emulator proof now covers:
    - teacher sign-in into the teacher-mobile shell
    - the teacher home to classrooms path
    - labeled teaching-scope selection for classroom create
    - classroom detail with short course summary
    - inline classroom title/course-code edit controls
    - archive affordance and explanatory copy
- Blockers:
  - no prompt-22 implementation blocker
  - a stale Docker API container initially hid the new assignment labels from Android validation,
    but rebuilding the API container resolved it and the refreshed emulator pass succeeded
- Exact next pickup point:
  - Prompt 23 in `Structure/phase_promptioing/20-product-reset-micro-prompts.md`

## Prompt 21 Summary

- Implemented:
  - redesigned the teacher mobile shell in `apps/mobile/src/teacher-foundation.tsx` around a real
    teacher home instead of scaffold-heavy dashboard copy
  - teacher home now shows:
    - a teacher-home status banner
    - a live-session spotlight card
    - a concise `Go To` action row
    - summary metrics
    - classroom highlight cards
    - recent attendance-session rows backed by the real teacher session-history API
  - switched teacher-home recent-session shaping from lecture-only fallback to live
    attendance-session history through:
    - `apps/mobile/src/teacher-models.ts`
    - `useTeacherAttendanceSessionsQuery()` inside `useTeacherDashboardData()`
  - added `apps/mobile/src/teacher-view-state.ts` so loading, empty, live-session, and ready-state
    copy now stays centralized and product-facing
  - added `teacherRoutes.home` in `apps/mobile/src/teacher-routes.ts` as the canonical teacher
    shell root alias while keeping `teacherRoutes.dashboard` compatible for existing internal calls
  - updated the teacher stack titles in `apps/mobile/app/(teacher)/_layout.tsx` so top-level
    screens now read:
    - `Home`
    - `Classrooms`
    - `Bluetooth Attendance`
    - `Live Bluetooth Session`
    - `Session History`
    - `Reports`
    - `Exports`
  - tightened top-level teacher screen subtitles to remove route-group and implementation-heavy
    language from:
    - teacher home
    - classrooms
    - classroom detail
    - session history
    - reports
    - exports
- Tests or validation strengthened:
  - `apps/mobile/src/teacher-models.test.ts`
  - `apps/mobile/src/teacher-routes.test.ts`
  - `apps/mobile/src/teacher-view-state.test.ts`
  - `Structure/testing-strategy.md` now records the teacher-shell prompt coverage expectations
- Commands run:
  - `pnpm exec biome check --write apps/mobile/src/teacher-foundation.tsx apps/mobile/src/teacher-models.ts apps/mobile/src/teacher-models.test.ts apps/mobile/src/teacher-routes.ts apps/mobile/src/teacher-routes.test.ts apps/mobile/src/teacher-view-state.ts apps/mobile/src/teacher-view-state.test.ts apps/mobile/app/'(teacher)'/_layout.tsx Structure/requirements/04-teacher-mobile-app.md Structure/architecture/04-teacher-mobile-app.md Structure/testing-strategy.md`
  - `pnpm test:mobile:targeted -- src/teacher-models.test.ts src/teacher-routes.test.ts src/teacher-view-state.test.ts`
  - `pnpm --filter @attendease/mobile typecheck`
  - `POSTGRES_PORT=55432 pnpm runtime:check`
  - `pnpm android:validate -- -d Pixel_9_Pro_XL --port 8093 --no-install`
  - `adb -s emulator-5554 shell "am start -W -a android.intent.action.VIEW -d 'attendease://expo-development-client/?url=http%3A%2F%2F192.168.29.11%3A8093' com.anurag203.attendease"`
  - `adb -s emulator-5554 shell input tap 700 2080`
  - `adb -s emulator-5554 shell input tap 670 1395`
  - `adb -s emulator-5554 shell input tap 280 2285`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt21-teacher-signin.png`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt21-teacher-home.png`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt21-teacher-classrooms.png`
  - `pnpm workspace:validate`
- Android validation evidence:
  - `Structure/artifacts/android-prompt21-teacher-signin.png`
  - `Structure/artifacts/android-prompt21-teacher-signin.xml`
  - `Structure/artifacts/android-prompt21-teacher-home.png`
  - `Structure/artifacts/android-prompt21-teacher-home.xml`
  - `Structure/artifacts/android-prompt21-teacher-classrooms.png`
  - `Structure/artifacts/android-prompt21-teacher-classrooms.xml`
  - emulator proof now covers:
    - teacher sign-in screen from the shared role-entry path
    - teacher home header with the new live-session spotlight
    - the simplified `Go To` action row
    - the `Home` stack title
    - the top-level `Classrooms` route with teacher-operational entry points visible
- Blockers:
  - no prompt-21 implementation blocker
  - native Android validation still inherits the known development-client constraint where direct
    nested teacher deep links can land on Expo Router's `Unmatched Route`; the working path
    remains root app launch plus in-app navigation
- Exact next pickup point:
  - Prompt 22 in `Structure/phase_promptioing/20-product-reset-micro-prompts.md`

## Prompt 17 Summary

- Implemented:
  - added a dedicated student `Classrooms` discovery route at `apps/mobile/app/(student)/classrooms/index.tsx`
  - extended `apps/mobile/src/student-routes.ts` with `studentRoutes.classrooms`
  - added shared student classroom-discovery modeling in `apps/mobile/src/student-workflow-models.ts` through:
    - `buildStudentCourseDiscoveryCards()`
    - `buildStudentClassroomDetailSummaryModel()`
  - added `useStudentCourseDiscoveryData()` and `StudentClassroomsScreen()` in `apps/mobile/src/student-foundation.tsx`
  - redesigned classroom discovery so students now see:
    - active classrooms first
    - course code and attendance-mode context
    - open-attendance indicators when a course has a live attendance session
    - next class-session context when attendance is closed
    - direct `Open course`, `Updates`, `Schedule`, and conditional `Attendance` entry points
  - redesigned classroom detail so the first card now summarizes:
    - whether attendance is open
    - next class session timing
    - update count
    - weekly-slot count
    - direct `Updates`, `Schedule`, and `Report` actions
  - updated the student home to preview only the top classrooms and route into the dedicated `Classrooms` screen through `All classrooms`
- Tests or validation strengthened:
  - `apps/mobile/src/student-workflow-models.test.ts`
  - `apps/mobile/src/student-routes.test.ts`
  - existing targeted student-shell tests were rerun through:
    - `apps/mobile/src/student-foundation.test.ts`
    - `apps/mobile/src/student-view-state.test.ts`
- Commands run:
  - `pnpm exec biome check --write apps/mobile/src/student-workflow-models.ts apps/mobile/src/student-workflow-models.test.ts apps/mobile/src/student-foundation.tsx apps/mobile/src/student-routes.ts apps/mobile/src/student-routes.test.ts apps/mobile/app/'(student)'/_layout.tsx apps/mobile/app/'(student)'/classrooms/index.tsx`
  - `pnpm test:mobile:targeted -- src/student-workflow-models.test.ts src/student-routes.test.ts src/student-foundation.test.ts src/student-view-state.test.ts`
  - `pnpm --filter @attendease/mobile typecheck`
  - `POSTGRES_PORT=55432 pnpm runtime:check`
  - `pnpm android:validate -- -d Pixel_9_Pro_XL --port 8091 --no-install`
  - `adb -s emulator-5554 shell input tap 302 2560`
  - `adb -s emulator-5554 shell input tap 281 1367`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt17-student-classrooms.png`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt17-student-course-detail.png`
  - `adb -s emulator-5554 shell uiautomator dump /sdcard/android-prompt17-student-classrooms.xml`
  - `adb -s emulator-5554 shell uiautomator dump /sdcard/android-prompt17-student-course-detail.xml`
  - `adb -s emulator-5554 logcat -d | rg "ReactNativeJS|FATAL EXCEPTION|Maximum update depth exceeded" -n`
  - `pnpm workspace:validate`
- Android validation evidence:
  - `Structure/artifacts/android-prompt17-student-classrooms.png`
  - `Structure/artifacts/android-prompt17-student-classrooms.xml`
  - `Structure/artifacts/android-prompt17-student-course-detail.png`
  - `Structure/artifacts/android-prompt17-student-course-detail.xml`
  - the emulator route sweep confirmed:
    - a dedicated `Classrooms` page is reachable from the home screen
    - enrolled classroom cards render with course code, next-session context, and direct actions
    - classroom detail renders recent updates, class-session context, and direct next actions
- Blockers:
  - no prompt-17 implementation blocker
  - open-attendance indicators are now implemented in code and covered by model tests; the current emulator seed snapshot for this pass showed next-session classroom states rather than a live open-attendance classroom card
- Exact next pickup point:
  - Prompt 18 in `Structure/phase_promptioing/20-product-reset-micro-prompts.md`

## Prompt 16 Summary

- Implemented:
  - redesigned the student mobile shell around a final product-first home experience instead of the older scaffold-style dashboard
  - refocused `StudentDashboardScreen` so the student sees:
    - a stateful home spotlight card
    - live open-attendance opportunities when available
    - enrolled classrooms as the main classroom hub
    - a smaller at-a-glance summary
    - simplified student navigation actions
  - updated `apps/mobile/src/student-models.ts` so the home experience now has stable shared model logic for:
    - spotlight state
    - classroom highlight rows
    - attendance-aware summary cards
  - updated `apps/mobile/src/student-view-state.ts` so student-home loading, no-classroom, attendance-open, and attendance-blocked states use short product copy
  - added `studentRoutes.home` and aligned mobile entry redirects to the student home route alias
  - tightened student stack header titles so the route group now reads as:
    - `Home`
    - `Attendance`
    - `Attendance History`
    - `Classroom`
    - `Updates`
    - `Schedule`
- Tests or validation strengthened:
  - `apps/mobile/src/student-foundation.test.ts`
  - `apps/mobile/src/student-view-state.test.ts`
  - `apps/mobile/src/student-routes.test.ts`
  - `apps/mobile/src/mobile-entry.test.ts`
- Commands run:
  - `pnpm exec biome check --write apps/mobile/src/student-models.ts apps/mobile/src/student-view-state.ts apps/mobile/src/student-view-state.test.ts apps/mobile/src/student-routes.ts apps/mobile/src/student-routes.test.ts apps/mobile/src/mobile-entry.tsx apps/mobile/src/mobile-entry-models.ts apps/mobile/src/student-foundation.tsx apps/mobile/src/student-foundation.test.ts apps/mobile/app/'(student)'/_layout.tsx`
  - `pnpm test:mobile:targeted -- src/student-foundation.test.ts src/student-view-state.test.ts src/student-routes.test.ts src/mobile-entry.test.ts`
  - `pnpm --filter @attendease/mobile typecheck`
  - `POSTGRES_PORT=55432 pnpm runtime:check`
  - `adb devices -l`
  - `pnpm android:validate -- -d Pixel_9_Pro_XL --port 8090 --no-install`
  - `adb -s emulator-5554 shell input tap 672 1139`
  - `adb -s emulator-5554 shell input tap 672 1579`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt16-student-home.png`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt16-student-signin.png`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt16-student-shell-home.png`
  - `adb -s emulator-5554 shell uiautomator dump /sdcard/android-prompt16-student-signin.xml`
  - `adb -s emulator-5554 shell uiautomator dump /sdcard/android-prompt16-student-shell-home.xml`
  - `pnpm workspace:validate`
- Blockers:
  - no prompt-16 implementation blocker
  - native Android validation succeeded, but the real-phone checks from the broader release-readiness track still remain outside the reset prompt
- Exact next pickup point:
  - Prompt 17 in `Structure/phase_promptioing/20-product-reset-micro-prompts.md`

## Prompt 15 Summary

- Implemented:
  - refreshed the reset-track DB fixture baseline so later prompts can reuse one shared source of truth for seeded people, classrooms, roster states, device lifecycle states, and registration payload builders
  - rewrote `packages/db/src/fixtures.ts` to expose:
    - richer seeded auth fixtures with display names and student profile data
    - reset-friendly classroom metadata under `developmentAcademicFixtures.classrooms`
    - `developmentLifecycleFixtures` for registration candidates, trusted/unregistered device states, roster lifecycle states, attendance truth, and admin recovery expectations
    - `buildDevelopmentDeviceFixture()`
    - `buildDevelopmentStudentRegistrationFixture()`
    - `buildDevelopmentTeacherRegistrationFixture()`
  - updated `packages/db/src/seed.ts` so seed data now uses the shared fixtures instead of duplicating more hard-coded UX data
  - strengthened the seeded baseline with realistic reset states:
    - student two now has historical replacement-device recovery data with one revoked legacy binding and one active replacement binding
    - physics now includes a dropped student membership and a blocked student membership
    - existing QR session truth for math remains stable for reporting and attendance prompts
  - updated `apps/api/src/test/integration-helpers.ts` so API integration suites now export reset-ready registration, roster, device-trust, and admin-recovery fixtures from the shared DB layer instead of inventing them locally
- Tests or validation strengthened:
  - `packages/db/src/fixtures.test.ts`
  - `packages/db/src/index.test.ts`
  - `packages/db/src/integration.test.ts`
  - `apps/api/src/test/integration-helpers.test.ts`
  - `Structure/testing-strategy.md`
- Commands run:
  - `pnpm exec biome check --write packages/db/src/fixtures.ts packages/db/src/fixtures.test.ts packages/db/src/index.test.ts packages/db/src/seed.ts packages/db/src/integration.test.ts apps/api/src/test/integration-helpers.ts apps/api/src/test/integration-helpers.test.ts`
  - `TEST_DATABASE_URL=postgresql://attendease:attendease@localhost:55432/attendease pnpm --filter @attendease/db test -- src/fixtures.test.ts src/index.test.ts src/integration.test.ts`
  - `pnpm --filter @attendease/api test -- src/test/integration-helpers.test.ts`
  - `pnpm --filter @attendease/db typecheck`
  - `pnpm --filter @attendease/api typecheck`
  - `POSTGRES_PORT=55432 pnpm test:api:integration -- src/modules/auth/auth.integration.test.ts src/modules/admin/admin-device-support.integration.test.ts`
  - `pnpm workspace:validate`
- Blockers:
  - no prompt-15 implementation blocker
  - targeted DB integration still emits the existing non-blocking `pg` deprecation warning during temporary-database setup
- Exact next pickup point:
  - Prompt 16 in `Structure/phase_promptioing/20-product-reset-micro-prompts.md`

## Prompt 14 Summary

- Implemented:
  - locked the reset-era destructive-action model so later UI prompts no longer need to guess whether delete means archive, revoke, drop, or hard delete
  - confirmed and documented the product-safe lifecycle rules:
    - semester delete => archive
    - classroom delete => archive
    - classroom-student remove => membership becomes `DROPPED`
    - join-code reset => previous code revoked
    - device delink/revoke => binding revoked
    - session history, attendance records, audit rows, exports, email logs, security events, admin actions, and outbox rows stay append-only from product UX
  - extended `AdminActionType` with:
    - `SEMESTER_ARCHIVE`
    - `CLASSROOM_ARCHIVE`
    - `CLASSROOM_STUDENT_REMOVE`
  - added `packages/db/prisma/migrations/20260315001200_destructive_action_audit_semantics/`
  - added `recordAdministrativeActionTrail()` in `packages/db/src/audit.ts` so admin-only destructive actions can persist admin-action rows plus outbox rows without duplicating transaction logic
  - updated academic services so:
    - semester archive writes `SEMESTER_ARCHIVE`
    - admin classroom archive writes `CLASSROOM_ARCHIVE`
    - admin classroom-student remove writes `CLASSROOM_STUDENT_REMOVE`
    - admin `PATCH` to `membershipStatus = DROPPED` is treated as the same destructive lifecycle for audit purposes, while new UI should still prefer the explicit `DELETE /classrooms/:id/students/:enrollmentId` route
  - synced the reset docs so announcement posts are explicitly append-only in reset v1 and session history is explicitly non-deletable from product UX
- Tests or validation strengthened:
  - `packages/db/src/audit.test.ts`
  - `packages/db/src/integration.test.ts`
  - `packages/db/src/index.test.ts`
  - `packages/db/src/schema.test.ts`
  - `packages/contracts/src/index.test.ts`
  - `apps/api/src/modules/academic/semesters.service.test.ts`
  - `apps/api/src/modules/academic/classrooms.service.test.ts`
  - `apps/api/src/modules/academic/roster.service.test.ts`
  - `apps/api/src/modules/academic/academic-management.integration.test.ts`
  - `apps/api/src/modules/academic/classroom-roster.integration.test.ts`
  - `Structure/testing-strategy.md`
- Commands run:
  - `pnpm exec biome check --write packages/contracts/src/devices.ts packages/db/prisma/schema.prisma packages/db/src/paths.ts packages/db/src/audit.ts packages/db/src/audit.test.ts packages/db/src/index.test.ts packages/db/src/schema.test.ts packages/db/src/integration.test.ts apps/api/src/test/integration-helpers.ts apps/worker/src/test-helpers.ts apps/api/src/modules/academic/semesters.service.ts apps/api/src/modules/academic/semesters.service.test.ts apps/api/src/modules/academic/classrooms.service.ts apps/api/src/modules/academic/classrooms.service.test.ts apps/api/src/modules/academic/roster.service.ts apps/api/src/modules/academic/roster.service.test.ts apps/api/src/modules/academic/academic-management.integration.test.ts apps/api/src/modules/academic/classroom-roster.integration.test.ts Structure/requirements/11-data-rules-audit.md Structure/architecture/11-data-rules-audit.md Structure/architecture/14-classroom-communications-and-roster.md Structure/architecture/15-device-trust-and-admin-controls.md Structure/requirements/08-session-history-manual-edits.md Structure/testing-strategy.md Structure/context.md`
  - `pnpm --filter @attendease/db prisma:validate`
  - `pnpm --filter @attendease/db prisma:generate`
  - `pnpm --filter @attendease/contracts test -- src/index.test.ts`
  - `pnpm --filter @attendease/db test -- src/audit.test.ts src/index.test.ts src/schema.test.ts`
  - `TEST_DATABASE_URL=postgresql://attendease:attendease@localhost:55432/attendease pnpm --filter @attendease/db exec vitest run src/integration.test.ts`
  - `pnpm --filter @attendease/api test -- src/modules/academic/semesters.service.test.ts src/modules/academic/classrooms.service.test.ts src/modules/academic/roster.service.test.ts`
  - `pnpm --filter @attendease/contracts typecheck`
  - `pnpm --filter @attendease/db typecheck`
  - `pnpm --filter @attendease/api typecheck`
  - `pnpm --filter @attendease/worker typecheck`
  - `POSTGRES_PORT=55432 pnpm test:api:integration -- src/modules/academic/academic-management.integration.test.ts src/modules/academic/classroom-roster.integration.test.ts`
  - `pnpm workspace:validate`
- Blockers:
  - no prompt-14 implementation blocker
  - targeted DB/API integration still emits the existing non-blocking `pg` deprecation warning during temporary-database setup
- Exact next pickup point:
  - Prompt 15 in `Structure/phase_promptioing/20-product-reset-micro-prompts.md`

## Prompt 13 Summary

- Implemented:
  - finished the reset-track classroom-student CRUD surface so teacher web, teacher mobile, and admin oversight can use one shared roster contract instead of platform-specific interpretations
  - extended `packages/contracts/src/academic.ts` so classroom-student responses now expose:
    - `studentName`
    - `studentIdentifier`
    - `memberSince`
    - `membershipState`
    - `actions`
  - extended classroom-student request and query contracts so shared clients can use:
    - `membershipStatus`
    - `studentIdentifier`
    - `studentRollNumber`
    - `studentUniversityId`
  - added the explicit remove route through `DELETE /classrooms/:classroomId/students/:enrollmentId`
  - updated `apps/api/src/modules/academic/roster.service.ts` so:
    - list uses the reset-friendly `membershipStatus` alias
    - manual add can resolve a student by identifier, roll number, university id, or email
    - roster rows now include API-provided block/remove/reactivate action flags
    - removal uses a dedicated classroom-student lifecycle path and emits `classroom.roster.member_removed`
  - updated `packages/auth/src/client.ts` so teacher web and teacher mobile can both call the same add, update, list, and remove helpers for classroom students
  - added `resolveStudentIdentifierLabel()` in `packages/domain/src/academic-language.ts` so product-facing identifier text stays consistent across API and later UI work
- Tests or validation strengthened:
  - `packages/domain/src/academic-language.test.ts`
  - `packages/domain/src/index.test.ts`
  - `packages/contracts/src/index.test.ts`
  - `packages/contracts/src/reset-contracts.test.ts`
  - `packages/auth/src/client.test.ts`
  - `packages/auth/src/reset-client.test.ts`
  - `apps/api/src/modules/academic/roster.service.test.ts`
  - `apps/api/src/modules/academic/classroom-roster.integration.test.ts`
  - `apps/web/src/web-auth-session.test.ts`
  - `Structure/testing-strategy.md` now records reset-era classroom-student alias and removal coverage
- Commands run:
  - `pnpm --filter @attendease/domain test -- src/academic-language.test.ts src/index.test.ts`
  - `pnpm --filter @attendease/contracts test -- src/index.test.ts src/reset-contracts.test.ts`
  - `pnpm --filter @attendease/auth test -- src/client.test.ts src/reset-client.test.ts`
  - `pnpm --filter @attendease/api test -- src/modules/academic/roster.service.test.ts`
  - `POSTGRES_PORT=55432 pnpm test:api:integration -- src/modules/academic/classroom-roster.integration.test.ts`
  - `pnpm --filter @attendease/domain typecheck`
  - `pnpm --filter @attendease/contracts typecheck`
  - `pnpm --filter @attendease/auth typecheck`
  - `pnpm --filter @attendease/api typecheck`
  - `pnpm --filter @attendease/mobile typecheck`
  - `pnpm --filter @attendease/web test -- src/web-auth-session.test.ts`
  - `pnpm --filter @attendease/web typecheck`
- Blockers:
  - no prompt-13 implementation blocker
  - later prompts still need to rebuild the actual teacher-mobile and teacher-web roster screens around this shared classroom-student contract, but the API/client surface is now stable
- Exact next pickup point:
  - Prompt 14 in `Structure/phase_promptioing/20-product-reset-micro-prompts.md`

## Prompt 12 Summary

- Implemented:
  - finished the reset-track classroom and course CRUD contract for teacher and admin workflows
  - extended `packages/contracts/src/academic.ts` so classroom create and edit requests now accept:
    - `courseCode`
    - `classroomTitle`
    - additive compatibility with existing `code` and `displayTitle`
  - extended classroom summary/detail responses so the API now exposes:
    - `courseCode`
    - `classroomTitle`
    - `semesterCode` and `semesterTitle`
    - `classCode` and `classTitle`
    - `sectionCode` and `sectionTitle`
    - `subjectCode` and `subjectTitle`
    - `primaryTeacherDisplayName`
    - `permissions`
  - updated `apps/api/src/modules/academic/classrooms.service.ts` so classroom create, edit, list,
    detail, and archive responses all return the same reset-ready shape
  - classroom create and edit now resolve the product-facing aliases into the existing backend
    storage fields instead of forcing later UI prompts to speak in internal terms
  - archive now rejects repeat archive attempts against already archived classrooms instead of
    silently succeeding
  - teacher responses now surface course-edit permissions but keep academic-scope and teacher
    reassignment permissions false
  - admin responses now surface the same classroom data with academic-scope and teacher
    reassignment permissions true
  - updated `packages/auth/src/client.ts` so shared classroom create and edit helpers validate the
    new reset request contracts before sending them
- Tests or validation strengthened:
  - `packages/domain/src/academic-language.test.ts`
  - `packages/domain/src/index.test.ts`
  - `packages/contracts/src/index.test.ts`
  - `packages/auth/src/client.test.ts`
  - `apps/api/src/modules/academic/classrooms.service.test.ts`
  - `apps/api/src/modules/academic/academic-management.integration.test.ts`
  - `Structure/testing-strategy.md` now records reset-era classroom CRUD contract coverage
- Commands run:
  - `pnpm exec biome check --write packages/contracts/src/academic.ts packages/contracts/src/index.test.ts packages/auth/src/client.ts packages/auth/src/client.test.ts apps/api/src/modules/academic/classrooms.service.ts apps/api/src/modules/academic/classrooms.service.test.ts apps/api/src/modules/academic/academic-management.integration.test.ts packages/domain/src/academic-language.ts packages/domain/src/academic-language.test.ts packages/domain/src/index.test.ts`
  - `pnpm --filter @attendease/contracts test -- src/index.test.ts`
  - `pnpm --filter @attendease/auth test -- src/client.test.ts`
  - `pnpm --filter @attendease/api test -- src/modules/academic/classrooms.service.test.ts`
  - `pnpm --filter @attendease/domain test -- src/academic-language.test.ts src/index.test.ts`
  - `pnpm --filter @attendease/contracts typecheck`
  - `pnpm --filter @attendease/auth typecheck`
  - `pnpm --filter @attendease/api typecheck`
  - `POSTGRES_PORT=55432 pnpm test:api:integration -- src/modules/academic/academic-management.integration.test.ts`
- Blockers:
  - no prompt-12 implementation blocker
  - later prompts still need to rebuild the actual teacher-mobile and teacher-web classroom CRUD
    screens around this contract, but the shared API shape and permission model are now stable
- Exact next pickup point:
  - Prompt 13 in `Structure/phase_promptioing/20-product-reset-micro-prompts.md`

## Prompt 11 Summary

- Implemented:
  - normalized the reset-track academic naming model across shared contracts, domain helpers, and academic API mappers
  - added `packages/domain/src/academic-language.ts` with the locked product glossary for:
    - `Classroom`
    - `Course code`
    - `Subject`
    - `Roster`
    - `Students`
    - `Class session`
    - `Attendance session`
  - added shared naming helpers for resolving:
    - classroom identity from `classroomId` or `courseOfferingId`
    - student membership identity from `membershipId` or `enrollmentId`
    - membership status and source aliases
    - class-session identity from `classSessionId` or `lectureId`
  - extended `packages/contracts/src/academic.ts` so product-facing academic payloads now expose classroom and membership aliases without breaking existing internal fields:
    - `studentClassroomMembershipSummarySchema` now includes `classroomId`
    - `enrollmentSummarySchema` now includes `classroomId` and `membershipId`
    - `classSessionListQuerySchema` and `createClassSessionRequestSchema` now alias the existing lecture list/create contracts
  - updated academic API mapping helpers so join-code, schedule, roster, student-classroom, class-session, announcement, enrollment, and roster-import responses now populate the new product-facing aliases
  - kept the current backend storage model intact so `courseOffering`, `enrollment`, and `lecture` remain implementation terms while UI layers can consume classroom-, membership-, and class-session-friendly payloads
  - synced the reset vocabulary in:
    - `Structure/ux-redesign-audit.md`
    - `Structure/architecture/13-academic-management-and-scheduling.md`
    - `Structure/architecture/14-classroom-communications-and-roster.md`
    - `Structure/requirements/03-student-mobile-app.md`
    - `Structure/requirements/04-teacher-mobile-app.md`
    - `Structure/requirements/05-teacher-web-app.md`
- Tests or validation strengthened:
  - `packages/domain/src/academic-language.test.ts`
  - `packages/domain/src/index.test.ts`
  - `packages/contracts/src/index.test.ts`
  - `apps/api/src/modules/academic/join-codes.service.test.ts`
  - `apps/api/src/modules/academic/roster.service.test.ts`
  - `apps/api/src/modules/academic/lectures.service.test.ts`
  - `apps/api/src/modules/academic/academic-management.integration.test.ts`
  - `apps/api/src/modules/academic/classroom-roster.integration.test.ts`
  - `Structure/testing-strategy.md` now records product-language and naming-alias coverage expectations
- Commands run:
  - `pnpm exec biome check --write packages/domain/src/academic-language.ts packages/domain/src/academic-language.test.ts packages/domain/src/index.ts packages/domain/src/index.test.ts packages/contracts/src/academic.ts packages/contracts/src/index.test.ts apps/api/src/modules/academic/classrooms.service.ts apps/api/src/modules/academic/join-codes.service.ts apps/api/src/modules/academic/roster.service.ts apps/api/src/modules/academic/lectures.service.ts apps/api/src/modules/academic/announcements.service.ts apps/api/src/modules/academic/enrollments.service.ts apps/api/src/modules/academic/roster-imports.service.ts apps/api/src/modules/academic/scheduling.service.ts apps/api/src/modules/academic/lectures.service.test.ts apps/api/src/modules/academic/roster.service.test.ts apps/api/src/modules/academic/join-codes.service.test.ts apps/api/src/modules/academic/academic-management.integration.test.ts apps/api/src/modules/academic/classroom-roster.integration.test.ts Structure/ux-redesign-audit.md Structure/architecture/13-academic-management-and-scheduling.md Structure/architecture/14-classroom-communications-and-roster.md Structure/requirements/03-student-mobile-app.md Structure/requirements/04-teacher-mobile-app.md Structure/requirements/05-teacher-web-app.md Structure/testing-strategy.md`
  - `pnpm --filter @attendease/contracts test -- src/index.test.ts`
  - `pnpm --filter @attendease/domain test -- src/academic-language.test.ts src/index.test.ts`
  - `pnpm --filter @attendease/api test -- src/modules/academic/join-codes.service.test.ts src/modules/academic/roster.service.test.ts src/modules/academic/lectures.service.test.ts src/modules/academic/announcements.service.test.ts src/modules/academic/enrollments.service.test.ts src/modules/academic/roster-imports.service.test.ts src/modules/academic/scheduling.service.test.ts`
  - `POSTGRES_PORT=55432 pnpm test:api:integration -- src/modules/academic/academic-management.integration.test.ts src/modules/academic/classroom-roster.integration.test.ts`
  - `pnpm --filter @attendease/contracts typecheck && pnpm --filter @attendease/domain typecheck && pnpm --filter @attendease/api typecheck && pnpm workspace:validate`
- Blockers:
  - no prompt-11 implementation blocker
  - current route and table names still use `courseOffering`, `enrollment`, and `lecture`, but the reset-track contract layer now exposes the product-facing aliases needed for later UI prompts
- Exact next pickup point:
  - Prompt 12 in `Structure/phase_promptioing/20-product-reset-micro-prompts.md`

## Locked Product Decisions For The Reset Track

- one shared mobile app with separate student and teacher entry
- teacher onboarding now uses registration plus sign-in on mobile and web
- teacher mobile owns Bluetooth attendance session creation
- teacher web owns QR + GPS attendance session creation
- student onboarding uses self-registration plus one-device binding
- admin is login-only and holds recovery/governance powers

These decisions must remain stable through the reset prompts.

## Prompt 10 Summary

- Implemented:
  - finished the reset-track student one-device lifecycle across contracts, backend policy, admin recovery, and mobile UX
  - extended `packages/contracts/src/auth.ts` so trusted-device context now carries a student-facing lifecycle state:
    - `NOT_APPLICABLE`
    - `UNREGISTERED`
    - `TRUSTED`
    - `PENDING_REPLACEMENT`
    - `REPLACED`
    - `BLOCKED`
  - extended `packages/contracts/src/devices.ts` so admin support responses now expose:
    - `attendanceDeviceState`
    - `pendingBinding`
  - updated `apps/api/src/modules/devices/device-binding-policy.service.ts` so:
    - student self-registration and login keep the first trusted phone active
    - same-phone multi-student attempts stay blocked
    - second-phone attempts for the same student create or reuse a pending replacement binding
    - revoked or replaced devices now return explicit lifecycle states instead of only vague blocked responses
  - updated `apps/api/src/modules/auth/auth.service.ts` so:
    - student registration returns lifecycle-aware trusted-device context
    - device conflicts reject cleanly before partial signup succeeds
    - student sign-in errors now distinguish pending replacement, replaced, blocked, and missing-device cases
  - updated `apps/api/src/modules/admin/admin-device-support.service.ts` so admin support summary/detail now distinguish:
    - the current attendance-device state
    - any pending replacement binding
  - updated `apps/mobile/src/device-trust.ts`, `apps/mobile/src/student-models.ts`, and `apps/mobile/src/student-foundation.tsx` so student attendance-sensitive flows and the dedicated device-status screen now render explicit `Trusted`, `Pending approval`, `Replaced`, `Blocked`, and `Unregistered` states
  - kept teacher and admin outside the strict student attendance-device gate
  - ran native Android validation on the debug build and captured:
    - student registration screen
    - student sign-in screen
    - student dashboard showing `Trusted Device = Trusted`
    - student device-status screen showing `Attendance device state: Trusted`
- Tests or validation strengthened:
  - `packages/contracts/src/reset-contracts.test.ts`
  - `packages/contracts/src/index.test.ts`
  - `packages/auth/src/reset-client.test.ts`
  - `packages/auth/src/client.test.ts`
  - `apps/api/src/modules/auth/auth.integration.test.ts`
  - `apps/api/src/modules/devices/device-binding-policy.service.test.ts`
  - `apps/api/src/modules/devices/trusted-device.guard.test.ts`
  - `apps/api/src/modules/devices/devices.integration.test.ts`
  - `apps/api/src/modules/admin/admin-device-support.integration.test.ts`
  - `apps/mobile/src/device-trust.test.ts`
  - `apps/mobile/src/student-foundation.test.ts`
  - `Structure/testing-strategy.md` now records the reset-era device-lifecycle coverage placement
- Commands run:
  - `pnpm exec biome check --write packages/contracts/src/auth.ts packages/contracts/src/devices.ts packages/contracts/src/reset-contracts.test.ts packages/contracts/src/index.test.ts packages/auth/src/reset-client.test.ts packages/auth/src/client.test.ts apps/api/src/modules/auth/auth.service.ts apps/api/src/modules/auth/auth.integration.test.ts apps/api/src/modules/devices/device-binding-policy.service.ts apps/api/src/modules/devices/device-binding-policy.service.test.ts apps/api/src/modules/devices/trusted-device.guard.test.ts apps/api/src/modules/devices/devices.integration.test.ts apps/api/src/modules/admin/admin-device-support.service.ts apps/api/src/modules/admin/admin-device-support.integration.test.ts apps/mobile/src/device-trust.ts apps/mobile/src/device-trust.test.ts apps/mobile/src/student-models.ts apps/mobile/src/student-foundation.tsx apps/mobile/src/student-foundation.test.ts Structure/requirements/02-auth-roles-enrollment.md Structure/requirements/03-student-mobile-app.md Structure/architecture/03-student-mobile-app.md Structure/architecture/15-device-trust-and-admin-controls.md Structure/testing-strategy.md README.md guide.md Structure/manual-check-quickstart.md Structure/context.md`
  - `pnpm --filter @attendease/contracts test -- src/reset-contracts.test.ts src/index.test.ts`
  - `pnpm --filter @attendease/auth test -- src/reset-client.test.ts src/client.test.ts`
  - `pnpm --filter @attendease/mobile test -- src/device-trust.test.ts src/student-foundation.test.ts src/student-attendance.test.ts src/student-workflow-models.test.ts src/teacher-models.test.ts src/teacher-session.test.ts`
  - `pnpm --filter @attendease/mobile typecheck`
  - `pnpm --filter @attendease/api test -- src/modules/devices/device-binding-policy.service.test.ts src/modules/devices/trusted-device.guard.test.ts`
  - `pnpm --filter @attendease/api typecheck`
  - `POSTGRES_PORT=55432 pnpm test:api:integration -- src/modules/auth/auth.integration.test.ts`
  - `POSTGRES_PORT=55432 pnpm test:api:integration -- src/modules/devices/devices.integration.test.ts`
  - `POSTGRES_PORT=55432 pnpm test:api:integration -- src/modules/admin/admin-device-support.integration.test.ts`
  - `pnpm --filter @attendease/contracts typecheck`
  - `pnpm --filter @attendease/auth typecheck`
  - `pnpm workspace:validate`
  - `POSTGRES_PORT=55432 pnpm runtime:check`
  - `pnpm android:validate:help`
  - `pnpm android:validate -- -d Pixel_9_Pro_XL --port 8088 --no-install`
  - `adb -s emulator-5554 shell am force-stop com.anurag203.attendease`
  - `adb -s emulator-5554 shell am start -W -a android.intent.action.VIEW -d 'attendease://expo-development-client/?url=http%3A%2F%2F192.168.29.11%3A8088' com.anurag203.attendease`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt10-student-dashboard-resigned.png`
  - `adb -s emulator-5554 exec-out screencap -p > Structure/artifacts/android-prompt10-device-status-trusted.png`
- Blockers:
  - no prompt-10 implementation blocker
  - the live Android proof initially showed stale trust data until the Docker API container was rebuilt; after rebuild and re-sign-in, the debug build showed the correct trusted-device lifecycle state
  - later prompts still need to redesign the broader student attendance and support surfaces around the reset UX, but the one-device lifecycle rules and support states are now stable
- Exact next pickup point:
  - Prompt 11 in `Structure/phase_promptioing/20-product-reset-micro-prompts.md`

## Prompt 9 Summary

- Implemented:
  - rebuilt the public web auth experience into three explicit role-owned routes:
    - `apps/web/app/login/page.tsx` for teacher sign in
    - `apps/web/app/register/page.tsx` for teacher registration
    - `apps/web/app/admin/login/page.tsx` for admin sign in
  - added `apps/web/src/web-auth-entry.tsx` to centralize:
    - teacher/admin auth entry copy
    - role-specific alternate links
    - role-specific error messages
    - shared teacher/admin auth screen layout
  - added dedicated teacher-registration and admin-login POST handlers:
    - `apps/web/app/register/password/route.ts`
    - `apps/web/app/admin/login/password/route.ts`
  - simplified `apps/web/app/login/password/route.ts` so it now owns teacher sign in only
  - removed the combined teacher/admin role picker and seeded-account-first framing from the primary web entry experience
  - tightened `apps/web/src/web-auth-session.ts` so web redirects now stay inside the matching role-owned route tree:
    - teacher auth stays inside `/teacher/...`
    - admin auth stays inside `/admin/...`
  - updated `apps/web/src/web-portal.ts` and `apps/web/src/web-shell.tsx` so protected teacher pages hand off to teacher sign in and protected admin pages hand off to admin sign in with explicit button labels
  - updated `apps/web/app/page.tsx` so the public web landing page now clearly separates teacher sign in, teacher account creation, and admin sign in
  - updated `README.md`, `guide.md`, and `Structure/manual-check-quickstart.md` so manual web validation follows the split teacher/admin auth paths
- Tests or validation strengthened:
  - `apps/web/src/web-auth-entry.test.ts`
  - `apps/web/src/web-auth-session.test.ts`
  - `apps/web/src/web-portal.test.ts`
  - `Structure/testing-strategy.md` now documents the reset-era web auth-entry and scoped web-session test coverage
- Commands run:
  - `pnpm exec biome check --write apps/web/src/web-auth-session.ts apps/web/src/web-auth-entry.tsx apps/web/src/web-portal.ts apps/web/src/web-shell.tsx apps/web/app/login/page.tsx apps/web/app/register/page.tsx apps/web/app/admin/login/page.tsx apps/web/app/page.tsx apps/web/app/login/password/route.ts apps/web/app/register/password/route.ts apps/web/app/admin/login/password/route.ts apps/web/src/web-auth-entry.test.ts apps/web/src/web-auth-session.test.ts apps/web/src/web-portal.test.ts Structure/requirements/05-teacher-web-app.md Structure/architecture/05-teacher-web-app.md Structure/requirements/02-auth-roles-enrollment.md Structure/architecture/02-auth-roles-enrollment.md Structure/testing-strategy.md README.md guide.md Structure/manual-check-quickstart.md`
  - `pnpm test:web:targeted -- src/auth.test.ts src/web-auth-entry.test.ts src/web-auth-session.test.ts src/web-portal.test.ts`
  - `pnpm --filter @attendease/web typecheck`
  - `pnpm --filter @attendease/web build`
  - `pnpm workspace:validate`
- Blockers:
  - no prompt-9 implementation blocker
  - production Google OIDC validation still belongs to the broader release-readiness track, not this reset prompt
  - later prompts still need to redesign the protected teacher and admin dashboard/workflow screens around the reset IA, but the web auth entry and cookie/session handoff are now stable
- Exact next pickup point:
  - Prompt 10 in `Structure/phase_promptioing/20-product-reset-micro-prompts.md`

## Prompt 6 Summary

- Implemented:
  - added a real student self-registration backend path at `POST /auth/register/student`
  - extended the shared auth contract so student registration now returns:
    - the authenticated student session
    - trusted-device context
    - `onboarding.recommendedNextStep`
  - implemented student account creation in `apps/api/src/modules/auth/auth.service.ts` with:
    - duplicate-email rejection
    - first-device preflight before account creation
    - student user creation
    - password credential creation
    - student role creation
    - student profile creation
    - first trusted device binding creation
    - active auth session + refresh token creation
    - `DEVICE_BOUND` security-event persistence
  - kept invalid or conflicting student registration requests from silently creating partial usable accounts
  - updated the reset auth and device-trust docs so student self-registration is now documented as a real product path instead of a later placeholder
  - updated `README.md`, `guide.md`, and `Structure/manual-check-quickstart.md` so manual verification now mentions fresh-email student signup in addition to seeded accounts
- Tests or validation strengthened:
  - `packages/contracts/src/reset-contracts.test.ts`
  - `packages/auth/src/reset-client.test.ts`
  - `apps/api/src/modules/auth/auth.integration.test.ts`
  - `packages/db/src/integration.test.ts`
- Commands run:
  - `pnpm exec biome check --write packages/contracts/src/auth.ts packages/contracts/src/reset-contracts.test.ts packages/auth/src/reset-client.test.ts apps/api/src/modules/auth/auth.controller.ts apps/api/src/modules/auth/auth.service.ts apps/api/src/modules/auth/auth.integration.test.ts packages/db/src/integration.test.ts`
  - `pnpm --filter @attendease/contracts test -- src/reset-contracts.test.ts`
  - `pnpm --filter @attendease/auth test -- src/reset-client.test.ts`
  - `pnpm --filter @attendease/api typecheck`
  - `POSTGRES_PORT=55432 pnpm test:api:integration -- src/modules/auth/auth.integration.test.ts`
  - `POSTGRES_PORT=55432 pnpm runtime:check`
  - `TEST_DATABASE_URL=postgresql://attendease:attendease@localhost:55432/attendease pnpm --filter @attendease/db test -- src/integration.test.ts`
- Blockers:
  - no prompt-6 blocker
  - no prompt-6 deferred implementation remains

## Prompt 7 Summary

- Implemented:
  - added a real teacher self-registration backend path at `POST /auth/register/teacher`
  - extended the shared auth contract so teacher registration now returns:
    - the authenticated teacher session
    - non-blocking teacher device-trust context
    - `onboarding.recommendedNextStep`
  - implemented teacher account creation in `apps/api/src/modules/auth/auth.service.ts` with:
    - duplicate-email rejection
    - explicit teacher user creation
    - password credential creation
    - explicit `TEACHER` role creation
    - teacher profile creation
    - immediate authenticated session + refresh token creation
    - optional device registration on mobile without student-only binding friction
  - kept admin provisioning private by leaving public admin registration unavailable
  - updated the matching auth, teacher-mobile, and teacher-web docs so teacher registration is now documented as a real mobile and web product path
  - updated `README.md`, `guide.md`, and `Structure/manual-check-quickstart.md` so manual verification now mentions fresh-email teacher signup in addition to the seeded teacher account
- Tests or validation strengthened:
  - `packages/contracts/src/reset-contracts.test.ts`
  - `packages/auth/src/reset-client.test.ts`
  - `apps/api/src/modules/auth/auth.integration.test.ts`
  - `apps/api/src/modules/auth/auth.service.test.ts`
  - `packages/db/src/integration.test.ts`
- Commands run:
  - `pnpm exec biome check --write packages/contracts/src/auth.ts packages/contracts/src/reset-contracts.test.ts packages/auth/src/reset-client.test.ts apps/api/src/modules/auth/auth.controller.ts apps/api/src/modules/auth/auth.service.ts apps/api/src/modules/auth/auth.integration.test.ts apps/api/src/modules/auth/auth.service.test.ts packages/db/src/integration.test.ts`
  - `pnpm --filter @attendease/contracts test -- src/reset-contracts.test.ts`
  - `pnpm --filter @attendease/auth test -- src/reset-client.test.ts`
  - `pnpm --filter @attendease/api test -- src/modules/auth/auth.service.test.ts`
  - `pnpm --filter @attendease/api typecheck`
  - `POSTGRES_PORT=55432 pnpm test:api:integration -- src/modules/auth/auth.integration.test.ts`
  - `TEST_DATABASE_URL=postgresql://attendease:attendease@localhost:55432/attendease pnpm --filter @attendease/db test -- src/integration.test.ts`
- Blockers:
  - no prompt-7 blocker
  - public teacher registration exists now, but later prompts still need to redesign the actual teacher mobile and web registration screens around this final backend path

## Prompt 8 Summary

- Implemented:
  - rebuilt the mobile unauthenticated entry flow so `apps/mobile/app/index.tsx` now opens a real role-choice landing screen instead of redirecting straight into the student route group
  - added dedicated shared-mobile auth routes under:
    - `apps/mobile/app/(entry)/student/sign-in.tsx`
    - `apps/mobile/app/(entry)/student/register.tsx`
    - `apps/mobile/app/(entry)/teacher/sign-in.tsx`
    - `apps/mobile/app/(entry)/teacher/register.tsx`
  - added `apps/mobile/src/mobile-entry.tsx` and `apps/mobile/src/mobile-entry-models.ts` to centralize:
    - role-entry landing-card state
    - protected-route gate decisions
    - auth-screen copy and helper state
  - extended `apps/mobile/src/student-session.tsx` so student mobile now owns:
    - explicit sign in
    - explicit self-registration
    - registration request building with device payload binding
    - prefilled dev credentials without silent auto sign-in
  - extended `apps/mobile/src/teacher-session.tsx` so teacher mobile now owns:
    - explicit sign in
    - explicit teacher self-registration
    - prefilled dev credentials without silent auto sign-in
  - updated `apps/mobile/app/(student)/_layout.tsx` and `apps/mobile/app/(teacher)/_layout.tsx` to gate protected student and teacher trees back to the correct sign-in route when the matching session is missing
  - removed the old raw sign-in forms from protected student and teacher surfaces and replaced them with lightweight “open the correct sign-in screen” fallback cards so the shared binary no longer exposes install-ID or public-key entry on normal auth paths
  - updated the shared mobile copy foundation in `apps/mobile/src/shell.ts` so sign-in and registration copy now exists for both student and teacher entry paths
  - updated `README.md`, `guide.md`, and `Structure/manual-check-quickstart.md` so manual validation now follows the new landing-screen-first flow
- Tests or validation strengthened:
  - `apps/mobile/src/mobile-entry.test.ts`
  - `apps/mobile/src/shell.test.ts`
  - `apps/mobile/src/student-session.test.ts`
  - `apps/mobile/src/teacher-session.test.ts`
  - `Structure/testing-strategy.md` now records the reset-track expectation that mobile auth prompts keep route-gate coverage in `mobile-entry.test.ts` and request-shape coverage in the role session tests
- Commands run:
  - `pnpm --filter @attendease/mobile test -- src/mobile-entry.test.ts src/shell.test.ts src/student-session.test.ts src/teacher-session.test.ts`
  - `pnpm test:mobile:targeted -- src/mobile-entry.test.ts src/shell.test.ts src/student-session.test.ts src/teacher-session.test.ts`
  - `pnpm --filter @attendease/mobile typecheck`
  - `pnpm exec biome check --write apps/mobile/src/shell.ts apps/mobile/src/shell.test.ts apps/mobile/src/student-session.tsx apps/mobile/src/student-session.test.ts apps/mobile/src/teacher-session.tsx apps/mobile/src/teacher-session.test.ts apps/mobile/src/mobile-entry-models.ts apps/mobile/src/mobile-entry.tsx apps/mobile/src/mobile-entry.test.ts apps/mobile/app/index.tsx apps/mobile/app/'(entry)'/_layout.tsx apps/mobile/app/'(entry)'/student/sign-in.tsx apps/mobile/app/'(entry)'/student/register.tsx apps/mobile/app/'(entry)'/teacher/sign-in.tsx apps/mobile/app/'(entry)'/teacher/register.tsx apps/mobile/app/'(student)'/_layout.tsx apps/mobile/app/'(teacher)'/_layout.tsx apps/mobile/src/student-foundation.tsx apps/mobile/src/teacher-foundation.tsx`
  - `POSTGRES_PORT=55432 pnpm runtime:check`
  - `pnpm workspace:validate`
  - `adb devices -l`
  - `pnpm android:validate:help`
  - `pnpm android:validate -- -d Pixel_9_Pro_XL --port 8086 --no-install`
  - `adb shell dumpsys activity activities | rg "mResumedActivity|topResumedActivity|attendease|expo-development-client"`
  - `adb exec-out screencap -p > Structure/artifacts/android-prompt8-entry-flow.png`
  - `adb exec-out screencap -p > Structure/artifacts/android-prompt8-teacher-register.png`
  - `adb exec-out screencap -p > Structure/artifacts/android-prompt8-student-signin.png`
  - `adb shell uiautomator dump /sdcard/window_dump.xml`
  - `adb logcat -d | rg "Maximum update depth exceeded|ReactNativeJS|FATAL EXCEPTION|Create student account|Choose your space|Teacher sign in|Student sign in"`
- Blockers:
  - no prompt-8 implementation blocker
  - native Android validation passed for the new landing screen plus student sign-in and teacher registration routes
  - Expo `run:android` required the AVD name `Pixel_9_Pro_XL`; the adb serial `emulator-5554` was not accepted by the Expo device selector
  - current native Android dev-client UI still shows the normal Expo warning strip (`Open debugger to view warnings.`); this is not a reset-track blocker for the auth flow itself

## Prompt 5 Summary

- Implemented:
  - stabilized the reset validation baseline by adding focused root commands for:
    - targeted API tests
    - targeted API integration tests
    - targeted mobile tests
    - targeted web tests
    - native Android validation help and execution
  - added a tested command runner in `scripts/run-targeted-command.mjs` and `scripts/targeted-command-lib.mjs` so pnpm-forwarded file filters now reach Vitest and Expo commands reliably
  - improved `apps/api/src/test/integration-helpers.ts` so targeted API integration tests can resolve PostgreSQL from `TEST_DATABASE_URL`, `DATABASE_URL`, `TEST_DATABASE_PORT`, or `POSTGRES_PORT`
  - added API helper coverage for that database-resolution logic
  - updated workspace validation so the Prompt 5 command scripts and the API `test:integration` script are now required baseline artifacts
  - updated `Structure/testing-strategy.md`, `README.md`, and `guide.md` with the reset-track targeted command strategy
- Tests or validation strengthened:
  - `apps/api/src/test/integration-helpers.test.ts`
  - `scripts/targeted-command-lib.test.ts`
  - root `pnpm test` now runs the targeted-command wrapper test before workspace package tests
- Commands run:
  - `pnpm exec biome check --write package.json apps/api/package.json apps/api/src/test/integration-helpers.ts apps/api/src/test/integration-helpers.test.ts scripts/targeted-command-lib.mjs scripts/run-targeted-command.mjs scripts/targeted-command-lib.test.ts`
  - `pnpm exec vitest run scripts/targeted-command-lib.test.ts`
  - `pnpm --filter @attendease/api test -- src/test/integration-helpers.test.ts`
  - `pnpm --filter @attendease/api typecheck`
  - `POSTGRES_PORT=55432 pnpm runtime:check`
  - `POSTGRES_PORT=55432 pnpm test:api:integration -- src/modules/auth/auth.integration.test.ts`
  - `pnpm test:mobile:targeted -- src/student-foundation.test.ts src/student-query.test.ts`
  - `pnpm test:web:targeted -- src/web-portal.test.ts`
  - `pnpm android:validate:help`
  - `pnpm workspace:validate`
- Blockers:
  - no prompt-5 blocker
  - `pg_isready` is not installed on this machine, so runtime/database health proof currently uses Docker/runtime checks rather than that binary

## Prompt 4 Summary

- Implemented:
  - added reset-specific shared contracts in `packages/contracts` for:
    - student registration
    - teacher registration
    - student, teacher, and admin password sign-in aliases
    - student and teacher Google exchange aliases
    - classroom-student list/add/update aliases
    - live attendance-session discovery filters and summaries
    - manual attendance action-based updates
    - student-support and device-recovery aliases
  - fixed the attendance-contract alias ordering and the Google exchange schema composition so the new reset contracts typecheck cleanly
  - extended `packages/auth/src/client.ts` with reset-friendly helper methods that either:
    - lock future registration endpoint paths, or
    - reuse the current live endpoints while mapping to the reset naming model
  - added reset-specific contract and client tests so later prompts can rely on the locked wire shapes
  - updated the auth requirement and architecture docs to document the reset contract boundary and migration foundation explicitly
  - updated `Structure/testing-strategy.md` and `scripts/validate-workspace.mjs` so Prompt 4 marker sections are now enforced alongside the earlier reset baseline markers
- Tests or validation strengthened:
  - `packages/contracts/src/reset-contracts.test.ts`
  - `packages/auth/src/reset-client.test.ts`
  - workspace validation now checks for `Reset Contract Boundary Requirements`, `Reset Contract Migration Foundation`, and `Prompt 4 Summary`
- Commands run:
  - `pnpm exec biome check --write packages/contracts/src/auth.ts packages/contracts/src/academic.ts packages/contracts/src/attendance.ts packages/contracts/src/devices.ts packages/contracts/src/reset-contracts.test.ts packages/auth/src/client.ts packages/auth/src/reset-client.test.ts`
  - `pnpm --filter @attendease/contracts test -- src/reset-contracts.test.ts`
  - `pnpm --filter @attendease/auth test -- src/reset-client.test.ts`
  - `pnpm --filter @attendease/auth typecheck`
  - `pnpm --filter @attendease/contracts typecheck`
- Blockers:
  - no prompt-4 blocker
  - registration endpoints are now contract-locked in the shared client, but the backend implementation still belongs to later reset prompts

## Prompt 3 Summary

- Implemented:
  - expanded `packages/ui-mobile` and `packages/ui-web` into reusable reset-era design foundations with shared typography, spacing, surface, CTA, and tone tokens
  - added shared content guardrails in those UI packages so mobile and web user-facing copy can be checked for developer-facing terms before later screen prompts land
  - extended `apps/mobile/src/shell.ts` with shared student/teacher sign-in copy builders and wired the current mobile sign-in cards to those helpers
  - refined shared first-run and portal surfaces in `apps/web/app/page.tsx`, `apps/web/app/login/page.tsx`, `apps/web/src/web-shell.tsx`, and `apps/web/src/web-portal.ts` so teacher/admin entry and shared page models use shorter, product-facing language
  - removed or reduced placeholder-heavy wording from the current shared mobile and web framing without changing the locked route tree or role boundaries
  - updated the matching student-mobile, teacher-mobile, and teacher-web requirement and architecture docs to record the shared visual/copy foundation for the reset track
  - updated `Structure/testing-strategy.md` so the shared UI package tests and shared portal-copy checks are part of the documented baseline
- Tests or validation strengthened:
  - `packages/ui-mobile/src/index.test.ts`
  - `packages/ui-web/src/index.test.ts`
  - `apps/mobile/src/shell.test.ts`
  - `apps/web/src/web-portal.test.ts`
- Commands run:
  - `pnpm exec biome check --write apps/mobile/src/shell.ts apps/mobile/src/shell.test.ts apps/mobile/src/student-foundation.tsx apps/mobile/src/teacher-foundation.tsx apps/web/app/page.tsx apps/web/app/login/page.tsx apps/web/src/web-shell.tsx apps/web/src/web-portal.test.ts`
  - `pnpm --filter @attendease/ui-mobile test -- src/index.test.ts`
  - `pnpm --filter @attendease/ui-web test -- src/index.test.ts`
  - `pnpm --filter @attendease/mobile test -- src/shell.test.ts`
  - `pnpm --filter @attendease/web test -- src/web-portal.test.ts`
  - `pnpm --filter @attendease/mobile typecheck`
  - `pnpm --filter @attendease/web typecheck`
  - `pnpm workspace:validate`
- Blockers:
  - no prompt-3 blocker
  - later reset prompts still need to redesign feature-specific screens that currently retain implementation-era copy outside the shared foundation layer

## Prompt 2 Summary

- Implemented:
  - locked the final first-run entry map for mobile and the separate auth ownership for student, teacher, and admin
  - documented final top-level navigation sections for student mobile, teacher mobile, teacher web, and admin web
  - normalized product-facing naming for classroom, course code, students, attendance session, present/absent, and device registration
  - updated the reset audit plus the matching auth, student-mobile, teacher-mobile, and teacher-web requirement/architecture docs to act as the IA source of truth
  - updated system-overview docs so the reset baseline now includes teacher registration plus sign-in on mobile and web
  - updated `Structure/testing-strategy.md` to note IA marker validation
  - strengthened `scripts/validate-workspace.mjs` so the reset-track audit, context, system-overview docs, and IA/auth docs are now enforced baseline artifacts
- Tests or validation strengthened:
  - automated workspace validation now fails if the reset audit is missing the final IA/naming sections or if the required IA/auth marker sections are missing from the matching requirement and architecture docs
- Commands run:
  - `pnpm exec biome check --write scripts/validate-workspace.mjs`
  - `pnpm workspace:validate`
- Blockers:
  - no prompt-2 documentation blocker
  - broader product blockers remain the existing release-readiness blockers

## Existing Baseline To Preserve

- Keep the approved stack in `Structure/final-tech-stack-and-implementation.md`
- Keep one-device binding and do not regress to MAC-address-only enforcement
- Keep teacher mobile as Bluetooth owner and teacher web as QR + GPS owner
- Reuse the existing backend/data foundations rather than rebuilding domain truth from scratch

## Exact Next Pickup Point

- Next prompt: `Structure/phase_promptioing/20-product-reset-micro-prompts.md`, Prompt 40
- Next task: execute the final reset-track validation and product-story closeout using the now-synced
  requirement, architecture, audit, README, guide, testing, and context docs as the source of truth
